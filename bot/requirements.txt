# What the bot needs to RUN. The data builders need more; see
# pipeline/requirements.txt — you only need those to rebuild data/.
discord.py>=2.4,<3  # Discord front-end (bot.py)
openai>=1.40        # OpenAI-compatible client, pointed at the DeepSeek API
python-dotenv>=1.0
fastapi>=0.110      # web front-end (app.py)
uvicorn>=0.29       # ASGI server for the web app
Pillow>=10.0        # pack images (draft.py) + stacked graft/augment cards (mods.py)
