// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] When I attack, if I am still in formation, create a 1/1 unit in all my empty adjacent slots."
// ggg/4 5/4 — Hooba Banana Unit
card('Hooba-Nan', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['attacked'], self: true,
    label: 'When I attack, if I am still in formation, create a 1/1 unit',
    effect: { run: (g, ctx) => { /* TODO: When I attack, if I am still in formation, create a 1/1 unit in all my empty adjacent slots. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Hooba-Nan: TODO — [Augment] When I attack, if I am still in formation, create ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] When I attack, if I am still in formation, create a 1/1 unit in all my empty adjacent slots.
  assert.fail('unwritten per-card test — definition of done');
});
