// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] {Unaware} Scrap Robot {Virus} Unit"
// m/2 2/2 — [Augment] {Unaware} Scrap Robot {Virus} Unit
// type-line [Augment]: augmenting grants {Unaware} — printed.augmentAttrs handles it, no behavior needed
card('Trashling', {});

// ── per-card test stub ────────────────────────────────────────────
test('Trashling: TODO — Unaware 2/2', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: (vanilla)
  assert.fail('unwritten per-card test — definition of done');
});
