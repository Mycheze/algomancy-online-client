// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever a player plays their first spell in this battle, negate it."
// be/5 6/7 — Polyform Primordial Beast Unit
card('Origon', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever a player plays their first spell in this battle, ne',
    effect: { run: (g, ctx) => { /* TODO: Whenever a player plays their first spell in this battle, negate it. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever a player plays their first spell in this …"

// ── per-card test stub ────────────────────────────────────────────
test('Origon: TODO — [Augment] Whenever a player plays their first spell in this ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever a player plays their first spell in this battle, negate it.
  assert.fail('unwritten per-card test — definition of done');
});
