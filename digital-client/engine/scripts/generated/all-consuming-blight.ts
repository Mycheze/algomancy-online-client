// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Create a Poison 1 for each of your units."
// gg/4 1/2 — Ancient Blight Spell
card('All-Consuming Blight', {
  spellEffect: {
    // TODO: Create a Poison 1 for each of your units.
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('All-Consuming Blight: TODO — Create a Poison 1 for each of your units.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Create a Poison 1 for each of your units.
  assert.fail('unwritten per-card test — definition of done');
});
