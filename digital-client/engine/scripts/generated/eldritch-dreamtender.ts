// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] When my column deals combat damage to an opponent, sacrifice me. If you do, look at that player's hand and discard a card from it."
// mm/1 1/1 — {Haste} {Evasive} Cosmic Alien Unit
card('Eldritch Dreamtender', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'When my column deals combat damage to an opponent, sacrifice',
    effect: { run: (g, ctx) => { /* TODO: When my column deals combat damage to an opponent, sacrifice me. If you do, look at that player's hand and discard a card from it. */ } },
  }],
});
// TODO(review): could not guess events for augment text "When my column deals combat damage to an opponent,…"

// ── per-card test stub ────────────────────────────────────────────
test('Eldritch Dreamtender: TODO — [Augment] When my column deals combat damage to an opponent,', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] When my column deals combat damage to an opponent, sacrifice me. If you do, look at that player's hand and discard a card from it.
  assert.fail('unwritten per-card test — definition of done');
});
