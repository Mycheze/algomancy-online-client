// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Spells with base cost [three] or less have a base cost of [three] to play during battle."
// be/3 2/4 — Arcane Beast Unit
card('Stasis Sentry', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Spells with base cost [three] or less have a base cost of [t',
    effect: { run: (g, ctx) => { /* TODO: Spells with base cost [three] or less have a base cost of [three] to play during battle. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Spells with base cost [three] or less have a base …"

// ── per-card test stub ────────────────────────────────────────────
test('Stasis Sentry: TODO — [Augment] Spells with base cost [three] or less have a base ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Spells with base cost [three] or less have a base cost of [three] to play during battle.
  assert.fail('unwritten per-card test — definition of done');
});
