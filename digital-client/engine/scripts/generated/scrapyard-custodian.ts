// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When you put one or more counters on an ally, [Switch1] Draw a card."
// me/3 3/3 — Robot Druid Unit
card('Scrapyard Custodian', {
  abilities: [{
    type: 'triggered', events: [], bounded: true, graftCause: true,
    label: 'Draw a card.',
    // TODO trigger clause: "When you put one or more counters on an ally" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Draw a card. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): could not guess events for "When you put one or more counters on an ally" — fill them in
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Scrapyard Custodian: TODO — When you put one or more counters on an ally, [Switch1] Draw', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When you put one or more counters on an ally, [Switch1] Draw a card.
  assert.fail('unwritten per-card test — definition of done');
});
