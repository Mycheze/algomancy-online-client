// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Target unit loses all attributes and abilities until regroup. Erase all of its mods and negate all of its effects."
// m/1 2/1 — {Battle} Technology Spell
card('Suppression Field', {
  spellEffect: {
    // TODO: Target unit loses all attributes and abilities until regroup. Erase all of its mods and negate all of its effects.
    targets: { what: 'unit', prompt: 'Suppression Field: Target unit loses all attributes and abilities until regroup. Erase all of its mods and negate all of its effects.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Suppression Field: TODO — Target unit loses all attributes and abilities until regroup', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Target unit loses all attributes and abilities until regroup. Erase all of its mods and negate all of its effects.
  assert.fail('unwritten per-card test — definition of done');
});
