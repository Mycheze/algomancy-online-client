// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch] /[Sacrifice a unit]: Create a Poison X and a Fireball X, where X is the defense of the sacrificed unit."
// rg/2 4/2 — {Battle} Infernal Blight Spell
card('Volatile Toxicity', {
  spellEffect: {
    // TODO: /[Sacrifice a unit]: Create a Poison X and a Fireball X, where X is the defense of the sacrificed unit.
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: false, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Volatile Toxicity: TODO — [Switch] /[Sacrifice a unit]: Create a Poison X and a Fireba', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch] /[Sacrifice a unit]: Create a Poison X and a Fireball X, where X is the defense of the sacrificed unit.
  assert.fail('unwritten per-card test — definition of done');
});
