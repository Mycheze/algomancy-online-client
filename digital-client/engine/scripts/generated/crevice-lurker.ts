// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Abilities cost [one] more to activate or trigger during battle. {i}(Choosing to not pay {/n}this prevents the abilities from triggering.)"
// ee/2 2/3 — Golem Beast Unit
card('Crevice Lurker', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Abilities cost [one] more to activate or trigger during batt',
    effect: { run: (g, ctx) => { /* TODO: Abilities cost [one] more to activate or trigger during battle. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Abilities cost [one] more to activate or trigger d…"

// ── per-card test stub ────────────────────────────────────────────
test('Crevice Lurker: TODO — [Augment] Abilities cost [one] more to activate or trigger d', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Abilities cost [one] more to activate or trigger during battle. {i}(Choosing to not pay {/n}this prevents the abilities from triggering.)
  assert.fail('unwritten per-card test — definition of done');
});
