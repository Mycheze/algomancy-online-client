// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] /[Put a -1/-1 counter on each enemy or{i1} put a{/n} +1/+1 counter on each of your units.]"
// gm/3 1/2 — {Battle} Arcane Druid Spell
card('Wither and Bloom', {
  spellEffect: {
    // TODO: /[Put a -1/-1 counter on each enemy or{i1} put a +1/+1 counter on each of your units.]
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Wither and Bloom: TODO — [Switch1] /[Put a -1/-1 counter on each enemy or{i1} put a +', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] /[Put a -1/-1 counter on each enemy or{i1} put a{/n} +1/+1 counter on each of your units.]
  assert.fail('unwritten per-card test — definition of done');
});
