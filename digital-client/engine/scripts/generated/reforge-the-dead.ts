// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Your units gain "When I die, create a Robot 3." until regroup."
// mm/3 5/6 — {Battle} Occult Technology Spell
card('Reforge the Dead', {
  spellEffect: {
    // TODO: Your units gain "When I die, create a Robot 3." until regroup.
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Reforge the Dead: TODO — Your units gain "When I die, create a Robot 3." until regrou', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Your units gain "When I die, create a Robot 3." until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
