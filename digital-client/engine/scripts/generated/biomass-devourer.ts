// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever a nontoken unit dies, you may pay [two] to erase it and put two +1/+1 counters on me."
// m/2 3/2 — Alien Robot Unit
card('Biomass Devourer', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever a nontoken unit dies, you may pay [two] to erase it',
    effect: { run: (g, ctx) => { /* TODO: Whenever a nontoken unit dies, you may pay [two] to erase it and put two +1/+1 counters on me. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever a nontoken unit dies, you may pay [two] t…"

// ── per-card test stub ────────────────────────────────────────────
test('Biomass Devourer: TODO — [Augment] Whenever a nontoken unit dies, you may pay [two] t', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever a nontoken unit dies, you may pay [two] to erase it and put two +1/+1 counters on me.
  assert.fail('unwritten per-card test — definition of done');
});
