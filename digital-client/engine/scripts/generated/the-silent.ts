// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Spells cost each player [two] more to play {/n}for each spell their team has previously {/n}played in this battle."
// em/3 3/4 — Cosmic Arcane Unit
card('The Silent', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Spells cost each player [two] more to play for each spell th',
    effect: { run: (g, ctx) => { /* TODO: Spells cost each player [two] more to play for each spell their team has previously played in this battle. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Spells cost each player [two] more to play for eac…"

// ── per-card test stub ────────────────────────────────────────────
test('The Silent: TODO — [Augment] Spells cost each player [two] more to play for eac', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Spells cost each player [two] more to play {/n}for each spell their team has previously {/n}played in this battle.
  assert.fail('unwritten per-card test — definition of done');
});
