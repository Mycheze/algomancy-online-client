// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When my column deals combat damage, {/n}[Switch1] Put a +1/+1 counter on each of your {/n}units."
// ee/3 1/3 — {Swift} Luminary Primordial Unit
card('Flowstone Arcanite', {
  abilities: [{
    type: 'triggered', events: [], bounded: true, graftCause: true,
    label: 'Put a +1/+1 counter on each of your units.',
    // TODO trigger clause: "When my column deals combat damage" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Put a +1/+1 counter on each of your units. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): could not guess events for "When my column deals combat damage" — fill them in
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Flowstone Arcanite: TODO — When my column deals combat damage, [Switch1] Put a +1/+1 co', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When my column deals combat damage, {/n}[Switch1] Put a +1/+1 counter on each of your {/n}units.
  assert.fail('unwritten per-card test — definition of done');
});
