// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] If you would create one or more unit tokens, instead create those tokens plus an additional copy of each unique token you created."
// mm/5 2/6 — Automaton Construct Unit
card('Automaton of Abundance', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'If you would create one or more unit tokens, instead create ',
    effect: { run: (g, ctx) => { /* TODO: If you would create one or more unit tokens, instead create those tokens plus an additional copy of each unique token you created. */ } },
  }],
});
// TODO(review): could not guess events for augment text "If you would create one or more unit tokens, inste…"

// ── per-card test stub ────────────────────────────────────────────
test('Automaton of Abundance: TODO — [Augment] If you would create one or more unit tokens, inste', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] If you would create one or more unit tokens, instead create those tokens plus an additional copy of each unique token you created.
  assert.fail('unwritten per-card test — definition of done');
});
