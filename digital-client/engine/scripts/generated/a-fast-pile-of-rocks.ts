// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I die, [Switch] Rockfall 4. {i}(Each player chooses one of their units. I deal 4 damage to each of the chosen units.)"
// e/2 2/1 — {Haste} Rock Pile Unit
card('A Fast Pile of Rocks', {
  abilities: [{
    type: 'triggered', events: ['died'], self: true, graftCause: true,
    label: 'Rockfall 4.',
    // TODO trigger clause: "When I die" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Rockfall 4. */ } },
  }],
  graftEffect: { bounded: false, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('A Fast Pile of Rocks: TODO — When I die, [Switch] Rockfall 4.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I die, [Switch] Rockfall 4. {i}(Each player chooses one of their units. I deal 4 damage to each of the chosen units.)
  assert.fail('unwritten per-card test — definition of done');
});
