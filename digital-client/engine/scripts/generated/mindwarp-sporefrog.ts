// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever you are dealt combat damage, target opponent gains control of me."
// bg/3 5/6 — Alien Frog Parasite {Virus} Unit
card('Mindwarp Sporefrog', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever you are dealt combat damage, target opponent gains ',
    effect: { run: (g, ctx) => { /* TODO: Whenever you are dealt combat damage, target opponent gains control of me. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever you are dealt combat damage, target oppon…"

// ── per-card test stub ────────────────────────────────────────────
test('Mindwarp Sporefrog: TODO — [Augment] Whenever you are dealt combat damage, target oppon', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever you are dealt combat damage, target opponent gains control of me.
  assert.fail('unwritten per-card test — definition of done');
});
