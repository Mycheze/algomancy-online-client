// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Your units are base 3/3."
// mm/3 3/3 — {Virus} {Unstable} Luminary Unit
card('Aberrant Statweaver', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Your units are base 3/3.',
    effect: { run: (g, ctx) => { /* TODO: Your units are base 3/3. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Your units are base 3/3.…"

// ── per-card test stub ────────────────────────────────────────────
test('Aberrant Statweaver: TODO — [Augment] Your units are base 3/3.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Your units are base 3/3.
  assert.fail('unwritten per-card test — definition of done');
});
