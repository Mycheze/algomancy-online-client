// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] At the start of deployment, recall all your other units."
// bg/1 4/4 — Alien Parasite Unit
card('Invasive Species', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'At the start of deployment, recall all your other units.',
    effect: { run: (g, ctx) => { /* TODO: At the start of deployment, recall all your other units. */ } },
  }],
});
// TODO(review): could not guess events for augment text "At the start of deployment, recall all your other …"

// ── per-card test stub ────────────────────────────────────────────
test('Invasive Species: TODO — [Augment] At the start of deployment, recall all your other ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] At the start of deployment, recall all your other units.
  assert.fail('unwritten per-card test — definition of done');
});
