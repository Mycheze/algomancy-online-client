// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Whenever a nontoken enemy dies, {/n} [Switch] Create a 1/1 unit."
// gg/2 1/2 — Fungus Druid Unit
card('Fungal Gardener', {
  abilities: [{
    type: 'triggered', events: [], graftCause: true,
    label: 'Create a 1/1 unit.',
    // TODO trigger clause: "Whenever a nontoken enemy dies" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Create a 1/1 unit. */ } },
  }],
  graftEffect: { bounded: false, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): could not guess events for "Whenever a nontoken enemy dies" — fill them in
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Fungal Gardener: TODO — Whenever a nontoken enemy dies, [Switch] Create a 1/1 unit.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Whenever a nontoken enemy dies, {/n} [Switch] Create a 1/1 unit.
  assert.fail('unwritten per-card test — definition of done');
});
