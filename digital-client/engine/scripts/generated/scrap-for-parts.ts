// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch] Move all counters from target unit onto another target unit."
// m/2 2/1 — {Battle} Scrap Spell
card('Scrap For Parts', {
  spellEffect: {
    // TODO: Move all counters from target unit onto another target unit.
    targets: { what: 'unit', prompt: 'Scrap For Parts: Move all counters from target unit onto another target unit.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: false, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Scrap For Parts: TODO — [Switch] Move all counters from target unit onto another tar', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch] Move all counters from target unit onto another target unit.
  assert.fail('unwritten per-card test — definition of done');
});
