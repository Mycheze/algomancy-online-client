// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Whenever a mod is applied to me, create {/n}an X/X unit, where X is the mod's cost."
// e/1 1/3 — Structure Unit
card('Perpetual Construct', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: Whenever a mod is applied to me, create an X/X unit, where X is the mod's cost. */ } },
  }],
});
// TODO(review): could not guess events for "Whenever a mod is applied to me, create an X/X uni…" — fill them in

// ── per-card test stub ────────────────────────────────────────────
test('Perpetual Construct: TODO — Whenever a mod is applied to me, create an X/X unit, where X', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Whenever a mod is applied to me, create {/n}an X/X unit, where X is the mod's cost.
  assert.fail('unwritten per-card test — definition of done');
});
