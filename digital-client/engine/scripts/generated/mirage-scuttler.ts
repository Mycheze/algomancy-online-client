// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever I survive damage, put that many +1/+1 counters on me."
// e/1 0/2 — Sand Crab Unit
card('Mirage Scuttler', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever I survive damage, put that many +1/+1 counters on m',
    effect: { run: (g, ctx) => { /* TODO: Whenever I survive damage, put that many +1/+1 counters on me. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever I survive damage, put that many +1/+1 cou…"

// ── per-card test stub ────────────────────────────────────────────
test('Mirage Scuttler: TODO — [Augment] Whenever I survive damage, put that many +1/+1 cou', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever I survive damage, put that many +1/+1 counters on me.
  assert.fail('unwritten per-card test — definition of done');
});
