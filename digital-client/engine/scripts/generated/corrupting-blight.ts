// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] After combat, a player of your choice who doesn't control me gains control of me."
// g/1 4/4 — Blight Parasite {Virus} Unit
card('Corrupting Blight', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['afterCombat'],
    label: 'After combat, a player of your choice who doesn\'t control m',
    effect: { run: (g, ctx) => { /* TODO: After combat, a player of your choice who doesn't control me gains control of me. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Corrupting Blight: TODO — [Augment] After combat, a player of your choice who doesn\'t ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] After combat, a player of your choice who doesn't control me gains control of me.
  assert.fail('unwritten per-card test — definition of done');
});
