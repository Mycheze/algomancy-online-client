// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Whenever you apply an augment during battle, [Switch] I gain +2/+2 until regroup."
// ee/2 2/3 — Mystic Luminary Unit
card('Morphic Mentor', {
  abilities: [{
    type: 'triggered', events: [], graftCause: true,
    label: 'I gain +2/+2 until regroup.',
    // TODO trigger clause: "Whenever you apply an augment during battle" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: I gain +2/+2 until regroup. */ } },
  }],
  graftEffect: { bounded: false, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): could not guess events for "Whenever you apply an augment during battle" — fill them in
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Morphic Mentor: TODO — Whenever you apply an augment during battle, [Switch] I gain', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Whenever you apply an augment during battle, [Switch] I gain +2/+2 until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
