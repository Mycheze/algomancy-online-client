// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] When I despawn, create a Crystal X, where X is my defense."
// be/3 1/3 — Mystic Primordial Pile Unit
card('A Pile of Runes', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'When I despawn, create a Crystal X, where X is my defense.',
    effect: { run: (g, ctx) => { /* TODO: When I despawn, create a Crystal X, where X is my defense. */ } },
  }],
});
// TODO(review): could not guess events for augment text "When I despawn, create a Crystal X, where X is my …"

// ── per-card test stub ────────────────────────────────────────────
test('A Pile of Runes: TODO — [Augment] When I despawn, create a Crystal X, where X is my ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] When I despawn, create a Crystal X, where X is my defense.
  assert.fail('unwritten per-card test — definition of done');
});
