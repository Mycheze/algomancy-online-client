// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Erase target unit. I become an exact copy of that unit. {i}(I copy all stat changes, counters, card text and mods)."
// mmm/7 2/2 — {Battle} Squid Mimic Spell Unit
card('Borrower of Forms', {
  spellEffect: {
    // TODO: Erase target unit. I become an exact copy of that unit. .
    targets: { what: 'unit', prompt: 'Borrower of Forms: Erase target unit. I become an exact copy of that unit. .' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Borrower of Forms: TODO — Erase target unit. I become an exact copy of that unit. .', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Erase target unit. I become an exact copy of that unit. {i}(I copy all stat changes, counters, card text and mods).
  assert.fail('unwritten per-card test — definition of done');
});
