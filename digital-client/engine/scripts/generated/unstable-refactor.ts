// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Target unit becomes base 5/0 until regroup."
// m/2 1/2 — {Battle} Cosmic Technology Spell
card('Unstable Refactor', {
  spellEffect: {
    // TODO: Target unit becomes base 5/0 until regroup.
    targets: { what: 'unit', prompt: 'Unstable Refactor: Target unit becomes base 5/0 until regroup.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Unstable Refactor: TODO — Target unit becomes base 5/0 until regroup.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Target unit becomes base 5/0 until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
