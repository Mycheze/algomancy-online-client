// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] /[Sacrifice a unit]: Each opponent sacri- {/n}fices units until their total defense is at least {/n}equal to the defense of your sacrificed unit."
// eer/3 1/6 — {Battle} Elemental Structure Spell
card('Structural Collapse', {
  spellEffect: {
    // TODO: /[Sacrifice a unit]: Each opponent sacri- fices units until their total defense is at least equal to the defense of your sacrificed unit.
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Structural Collapse: TODO — [Switch1] /[Sacrifice a unit]: Each opponent sacri- fices un', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] /[Sacrifice a unit]: Each opponent sacri- {/n}fices units until their total defense is at least {/n}equal to the defense of your sacrificed unit.
  assert.fail('unwritten per-card test — definition of done');
});
