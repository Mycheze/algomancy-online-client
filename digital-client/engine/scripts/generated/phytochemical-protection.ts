// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Until regroup, prevent all damage that {/n}would be dealt to target unit. Put a +1/+1 {/n}counter on it for each damage prevented {/n}this way."
// gg/2 3/4 — {Battle} Plant Spell
card('Phytochemical Protection', {
  spellEffect: {
    // TODO: Until regroup, prevent all damage that would be dealt to target unit. Put a +1/+1 counter on it for each damage prevented this way.
    targets: { what: 'unit', prompt: 'Phytochemical Protection: Until regroup, prevent all damage that would be dealt to target unit. Put a +1/+1 counter on it for each damage prevented this way.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Phytochemical Protection: TODO — Until regroup, prevent all damage that would be dealt to tar', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Until regroup, prevent all damage that {/n}would be dealt to target unit. Put a +1/+1 {/n}counter on it for each damage prevented {/n}this way.
  assert.fail('unwritten per-card test — definition of done');
});
