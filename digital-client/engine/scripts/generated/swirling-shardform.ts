// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I spawn, create two Shards. {i}(Dormant)"
// e/2 2/1 — Primordial Maelstrom Unit
card('Swirling Shardform', {
  abilities: [{
    type: 'triggered', events: ['spawned'], self: true,
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: When I spawn, create two Shards. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Swirling Shardform: TODO — When I spawn, create two Shards.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I spawn, create two Shards. {i}(Dormant)
  assert.fail('unwritten per-card test — definition of done');
});
