// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Target unit gains +1/+1 for each of your units until regroup."
// g/2 3/4 — {Battle}Tree Tree Druid Spell
card('Might of the Grove', {
  spellEffect: {
    // TODO: Target unit gains +1/+1 for each of your units until regroup.
    targets: { what: 'unit', prompt: 'Might of the Grove: Target unit gains +1/+1 for each of your units until regroup.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Might of the Grove: TODO — [Switch1] Target unit gains +1/+1 for each of your units unt', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Target unit gains +1/+1 for each of your units until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
