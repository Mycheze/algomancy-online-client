// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] [one], Remove X +1/+1 counters from me: I deal X damage to target unit."
// mm/3 2/3 — Demon Technology {Virus} Unit
card('Soul Reaver', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: '[one], Remove X +1/+1 counters from me: I deal X damage to t',
    effect: { run: (g, ctx) => { /* TODO: [one], Remove X +1/+1 counters from me: I deal X damage to target unit. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[one], Remove X +1/+1 counters from me: I deal X d…"

// ── per-card test stub ────────────────────────────────────────────
test('Soul Reaver: TODO — [Augment] [one], Remove X +1/+1 counters from me: I deal X d', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] [one], Remove X +1/+1 counters from me: I deal X damage to target unit.
  assert.fail('unwritten per-card test — definition of done');
});
