// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "End this battle. Erase this spell. {i}(Negate all effects, this battle is over.)"
// bmm/4 1/2 — {Battle} Temporal Arcane Spell
card('Temporal Rift', {
  spellEffect: {
    // TODO: End this battle. Erase this spell.
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Temporal Rift: TODO — End this battle. Erase this spell.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: End this battle. Erase this spell. {i}(Negate all effects, this battle is over.)
  assert.fail('unwritten per-card test — definition of done');
});
