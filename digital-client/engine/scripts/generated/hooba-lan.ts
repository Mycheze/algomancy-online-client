// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] When I attack or block, create a Shard. {i}(It will spawn dormant.)"
// eee/3 3/4 — Hooba Rock Unit
card('Hooba-Lan', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['attacked', 'blocked'], self: true,
    label: 'When I attack or block, create a Shard.',
    effect: { run: (g, ctx) => { /* TODO: When I attack or block, create a Shard. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Hooba-Lan: TODO — [Augment] When I attack or block, create a Shard.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] When I attack or block, create a Shard. {i}(It will spawn dormant.)
  assert.fail('unwritten per-card test — definition of done');
});
