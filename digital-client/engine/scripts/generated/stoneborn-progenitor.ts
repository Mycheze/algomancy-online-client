// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When one of your units survives damage, [Switch1] Create a 2/2 unit."
// ee/2 1/3 — Mystic Rock Unit
card('Stoneborn Progenitor', {
  abilities: [{
    type: 'triggered', events: [], bounded: true, graftCause: true,
    label: 'Create a 2/2 unit.',
    // TODO trigger clause: "When one of your units survives damage" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Create a 2/2 unit. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): could not guess events for "When one of your units survives damage" — fill them in
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Stoneborn Progenitor: TODO — When one of your units survives damage, [Switch1] Create a 2', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When one of your units survives damage, [Switch1] Create a 2/2 unit.
  assert.fail('unwritten per-card test — definition of done');
});
