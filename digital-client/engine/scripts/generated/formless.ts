// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I attack or block, [Switch] Target unit be- {/n}comes a base 4/4 and loses all attributes until regroup. {i}(This removes attributes from its column.)"
// mm/3 2/2 — {Flying} Cloud Avatar Unit
card('Formless', {
  abilities: [{
    type: 'triggered', events: ['attacked', 'blocked'], self: true, graftCause: true,
    label: 'Target unit be- comes a base 4/4 and loses all attributes un',
    // TODO trigger clause: "When I attack or block" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Target unit be- comes a base 4/4 and loses all attributes until regroup. */ } },
  }],
  graftEffect: { bounded: false, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Formless: TODO — When I attack or block, [Switch] Target unit be- comes a bas', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I attack or block, [Switch] Target unit be- {/n}comes a base 4/4 and loses all attributes until regroup. {i}(This removes attributes from its column.)
  assert.fail('unwritten per-card test — definition of done');
});
