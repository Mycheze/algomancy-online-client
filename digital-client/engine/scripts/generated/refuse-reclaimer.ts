// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever another unit dies, put a +1/+1 counter on me."
// m/1 1/1 — Occult Scrap Unit
card('Refuse Reclaimer', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever another unit dies, put a +1/+1 counter on me.',
    effect: { run: (g, ctx) => { /* TODO: Whenever another unit dies, put a +1/+1 counter on me. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever another unit dies, put a +1/+1 counter on…"

// ── per-card test stub ────────────────────────────────────────────
test('Refuse Reclaimer: TODO — [Augment] Whenever another unit dies, put a +1/+1 counter on', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever another unit dies, put a +1/+1 counter on me.
  assert.fail('unwritten per-card test — definition of done');
});
