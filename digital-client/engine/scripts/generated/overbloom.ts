// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Target unit gains +7/+7 until regroup."
// g/2 1/2 — Flower Spell
card('Overbloom', {
  spellEffect: {
    // TODO: Target unit gains +7/+7 until regroup.
    targets: { what: 'unit', prompt: 'Overbloom: Target unit gains +7/+7 until regroup.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Overbloom: TODO — [Switch1] Target unit gains +7/+7 until regroup.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Target unit gains +7/+7 until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
