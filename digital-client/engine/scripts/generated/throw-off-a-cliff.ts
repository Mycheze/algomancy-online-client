// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Delete target unit with 4 or more defense."
// e/2 2/4 — {Battle} Rock Spell
card('Throw off a Cliff', {
  spellEffect: {
    // TODO: Delete target unit with 4 or more defense.
    targets: { what: 'unit', prompt: 'Throw off a Cliff: Delete target unit with 4 or more defense.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Throw off a Cliff: TODO — Delete target unit with 4 or more defense.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Delete target unit with 4 or more defense.
  assert.fail('unwritten per-card test — definition of done');
});
