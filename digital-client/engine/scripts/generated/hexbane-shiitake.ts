// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment][once] Whenever another player plays a spell, you may exchange control of me for that spell. If you do, you may choose new targets for that spell."
// g/4 3/3 — Arcane Fungus Unit
card('Hexbane Shiitake', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['spellPlayed'], bounded: true,   // [once]
    label: 'Whenever another player plays a spell, you may exchange cont',
    effect: { run: (g, ctx) => { /* TODO: Whenever another player plays a spell, you may exchange control of me for that spell. If you do, you may choose new targets for that spell. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Hexbane Shiitake: TODO — [Augment][once] Whenever another player plays a spell, you m', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment][once] Whenever another player plays a spell, you may exchange control of me for that spell. If you do, you may choose new targets for that spell.
  assert.fail('unwritten per-card test — definition of done');
});
