// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] When I die, put a -1/-1 counter on each unit."
// gg/3 4/2 — Blight Fungus {Virus} Unit
card('Noxious Deathcap', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['died'], self: true,
    label: 'When I die, put a -1/-1 counter on each unit.',
    effect: { run: (g, ctx) => { /* TODO: When I die, put a -1/-1 counter on each unit. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Noxious Deathcap: TODO — [Augment] When I die, put a -1/-1 counter on each unit.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] When I die, put a -1/-1 counter on each unit.
  assert.fail('unwritten per-card test — definition of done');
});
