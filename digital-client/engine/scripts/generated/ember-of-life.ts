// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment][once] When one of your spell effects deals damage, create that many 1/1 units."
// rg/4 2/2 — Plant Elemental Unit
card('Ember of Life', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [], bounded: true,   // [once]
    label: 'When one of your spell effects deals damage, create that man',
    effect: { run: (g, ctx) => { /* TODO: When one of your spell effects deals damage, create that many 1/1 units. */ } },
  }],
});
// TODO(review): could not guess events for augment text "When one of your spell effects deals damage, creat…"

// ── per-card test stub ────────────────────────────────────────────
test('Ember of Life: TODO — [Augment][once] When one of your spell effects deals damage,', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment][once] When one of your spell effects deals damage, create that many 1/1 units.
  assert.fail('unwritten per-card test — definition of done');
});
