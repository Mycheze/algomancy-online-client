// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "If I spawned this turn, other units lose all attributes and abilities during battle."
// m/1 1/1 — {Battle} Bedlam Alien Monkey Unit
card('Monke', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: If I spawned this turn, other units lose all attributes and abilities during battle. */ } },
  }],
});
// TODO(review): could not guess events for "If I spawned this turn, other units lose all attri…" — fill them in

// ── per-card test stub ────────────────────────────────────────────
test('Monke: TODO — If I spawned this turn, other units lose all attributes and ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: If I spawned this turn, other units lose all attributes and abilities during battle.
  assert.fail('unwritten per-card test — definition of done');
});
