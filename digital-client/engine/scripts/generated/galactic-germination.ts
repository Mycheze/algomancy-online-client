// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Create a 1/1 unit for each unit in target formation."
// bg/3 5/1 — {Battle} Alien Fungus Spell
card('Galactic Germination', {
  spellEffect: {
    // TODO: Create a 1/1 unit for each unit in target formation.
    targets: { what: 'unit', prompt: 'Galactic Germination: Create a 1/1 unit for each unit in target formation.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Galactic Germination: TODO — Create a 1/1 unit for each unit in target formation.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Create a 1/1 unit for each unit in target formation.
  assert.fail('unwritten per-card test — definition of done');
});
