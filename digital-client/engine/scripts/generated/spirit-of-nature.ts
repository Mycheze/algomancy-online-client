// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "After combat, [Switch1] Create a /[Poison {i1}or Crystal] 2."
// eg/3 2/3 — Plant Crystal Unit
card('Spirit of Nature', {
  abilities: [{
    type: 'triggered', events: ['afterCombat'], bounded: true, graftCause: true,
    label: 'Create a /[Poison {i1}or Crystal] 2.',
    // TODO trigger clause: "After combat" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Create a /[Poison {i1}or Crystal] 2. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Spirit of Nature: TODO — After combat, [Switch1] Create a /[Poison {i1}or Crystal] 2.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: After combat, [Switch1] Create a /[Poison {i1}or Crystal] 2.
  assert.fail('unwritten per-card test — definition of done');
});
