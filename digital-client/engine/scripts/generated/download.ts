// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Gain control of target token. You may choose new targets for spells controlled this way."
// mm/1 4/3 — {Battle} Technology Spell
card('Download', {
  spellEffect: {
    // TODO: Gain control of target token. You may choose new targets for spells controlled this way.
    targets: { what: 'unit', prompt: 'Download: Gain control of target token. You may choose new targets for spells controlled this way.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Download: TODO — Gain control of target token. You may choose new targets for', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Gain control of target token. You may choose new targets for spells controlled this way.
  assert.fail('unwritten per-card test — definition of done');
});
