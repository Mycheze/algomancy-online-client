// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] After combat, I deal damage equal to my defense to each opponent."
// eee/3 2/3 — Alien Strider Unit
card('Deathglow Strider', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['afterCombat'],
    label: 'After combat, I deal damage equal to my defense to each oppo',
    effect: { run: (g, ctx) => { /* TODO: After combat, I deal damage equal to my defense to each opponent. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Deathglow Strider: TODO — [Augment] After combat, I deal damage equal to my defense to', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] After combat, I deal damage equal to my defense to each opponent.
  assert.fail('unwritten per-card test — definition of done');
});
