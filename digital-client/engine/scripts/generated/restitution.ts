// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever I am I dealt damage, I deal that much damage to each opponent."
// e/4 4/4 — Occult Avatar Unit
card('Restitution', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever I am I dealt damage, I deal that much damage to eac',
    effect: { run: (g, ctx) => { /* TODO: Whenever I am I dealt damage, I deal that much damage to each opponent. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever I am I dealt damage, I deal that much dam…"

// ── per-card test stub ────────────────────────────────────────────
test('Restitution: TODO — [Augment] Whenever I am I dealt damage, I deal that much dam', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever I am I dealt damage, I deal that much damage to each opponent.
  assert.fail('unwritten per-card test — definition of done');
});
