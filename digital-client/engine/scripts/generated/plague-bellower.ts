// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] [three]: Each opponent chooses one of their units. Put a -1/-1 counter on each of the chosen units."
// g/2 2/2 — Insect Plant Unit
card('Plague Bellower', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: '[three]: Each opponent chooses one of their units. Put a -1/',
    effect: { run: (g, ctx) => { /* TODO: [three]: Each opponent chooses one of their units. Put a -1/-1 counter on each of the chosen units. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[three]: Each opponent chooses one of their units.…"

// ── per-card test stub ────────────────────────────────────────────
test('Plague Bellower: TODO — [Augment] [three]: Each opponent chooses one of their units.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] [three]: Each opponent chooses one of their units. Put a -1/-1 counter on each of the chosen units.
  assert.fail('unwritten per-card test — definition of done');
});
