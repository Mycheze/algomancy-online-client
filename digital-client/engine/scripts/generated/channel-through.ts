// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "I deal 2 damage to each of X target allies. For each ally damaged this way, distribute 2 damage among target opponent's units."
// eer/X 0/6 — {Battle} Elemental Spell
card('Channel Through', {
  spellEffect: {
    // TODO: I deal 2 damage to each of X target allies. For each ally damaged this way, distribute 2 damage among target opponent's units.
    targets: { what: 'unit', prompt: 'Channel Through: I deal 2 damage to each of X target allies. For each ally damaged this way, distribute 2 damage among target opponent\'s units.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Channel Through: TODO — I deal 2 damage to each of X target allies. For each ally da', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: I deal 2 damage to each of X target allies. For each ally damaged this way, distribute 2 damage among target opponent's units.
  assert.fail('unwritten per-card test — definition of done');
});
