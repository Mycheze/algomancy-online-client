// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Everything is {g}deadly. {i}(Any damage from a deadly source will kill a unit.)"
// gr/2 2/2 — Blight Spider Unit
card('Rotspore Herald', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Everything is {g}deadly.',
    effect: { run: (g, ctx) => { /* TODO: Everything is {g}deadly. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Everything is {g}deadly.…"

// ── per-card test stub ────────────────────────────────────────────
test('Rotspore Herald: TODO — [Augment] Everything is {g}deadly.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Everything is {g}deadly. {i}(Any damage from a deadly source will kill a unit.)
  assert.fail('unwritten per-card test — definition of done');
});
