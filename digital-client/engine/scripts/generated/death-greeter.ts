// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever you play a spell during battle, each player sacrifices a unit with cost less than or equal to the spell's cost. {i}(If able.)"
// rb/3 4/2 — Elemental Spirit Unit
card('Death Greeter', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['spellPlayed'],
    label: 'Whenever you play a spell during battle, each player sacrifi',
    effect: { run: (g, ctx) => { /* TODO: Whenever you play a spell during battle, each player sacrifices a unit with cost less than or equal to the spell's cost. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Death Greeter: TODO — [Augment] Whenever you play a spell during battle, each play', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever you play a spell during battle, each player sacrifices a unit with cost less than or equal to the spell's cost. {i}(If able.)
  assert.fail('unwritten per-card test — definition of done');
});
