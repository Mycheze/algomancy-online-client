// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] [two]: Change a target of target effect to me."
// e/1 1/2 — Spirit Guardian Unit
card('Enigmatic Warder', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: '[two]: Change a target of target effect to me.',
    effect: { run: (g, ctx) => { /* TODO: [two]: Change a target of target effect to me. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[two]: Change a target of target effect to me.…"

// ── per-card test stub ────────────────────────────────────────────
test('Enigmatic Warder: TODO — [Augment] [two]: Change a target of target effect to me.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] [two]: Change a target of target effect to me.
  assert.fail('unwritten per-card test — definition of done');
});
