// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Your units gain +2/+2 and {g}piercing until regroup. {i}(Excess damage from piercing sources is dealt to the recipient's controller.)"
// gg/4 1/5 — Alien Druid Spell
card('Pernicious Photosynthesis', {
  spellEffect: {
    // TODO: Your units gain +2/+2 and {g}piercing until regroup.
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Pernicious Photosynthesis: TODO — [Switch1] Your units gain +2/+2 and {g}piercing until regrou', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Your units gain +2/+2 and {g}piercing until regroup. {i}(Excess damage from piercing sources is dealt to the recipient's controller.)
  assert.fail('unwritten per-card test — definition of done');
});
