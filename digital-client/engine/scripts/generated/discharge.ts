// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] /[Remove X +1/+1 counters from allies]: I deal X damage to target unit."
// m/1 0/0 — {Battle} Elemental Technology Spell
card('Discharge', {
  spellEffect: {
    // TODO: /[Remove X +1/+1 counters from allies]: I deal X damage to target unit.
    targets: { what: 'unit', prompt: 'Discharge: /[Remove X +1/+1 counters from allies]: I deal X damage to target unit.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Discharge: TODO — [Switch1] /[Remove X +1/+1 counters from allies]: I deal X d', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] /[Remove X +1/+1 counters from allies]: I deal X damage to target unit.
  assert.fail('unwritten per-card test — definition of done');
});
