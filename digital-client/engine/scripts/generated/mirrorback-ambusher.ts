// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Battle] Ambush [2be] {i}(Play me with the effect "Recall target ally, put me into their position in play."){/i} {/n}[once] When I am dealt damage, I deal that much damage to target unit."
// be/2 1/1 — Arcane Crab Unit
// has [Battle] Ambush — handled by the engine via printed.ambush; no behavior needed for the mode itself
card('Mirrorback Ambusher', {
  abilities: [{
    type: 'triggered', events: ['damage'], self: true,
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: [Battle] Ambush [2be] [once] When I am dealt damage, I deal that much damage to target unit. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Mirrorback Ambusher: TODO — [Battle] Ambush [2be] [once] When I am dealt damage, I deal ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Battle] Ambush [2be] {i}(Play me with the effect "Recall target ally, put me into their position in play."){/i} {/n}[once] When I am dealt damage, I deal that much damage to target unit.
  assert.fail('unwritten per-card test — definition of done');
});
