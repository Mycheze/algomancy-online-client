// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I spawn or become modded, put a +1/+1 counter on each of your units. {/n}[Augment] When I despawn, remove all counters from your units."
// m/2 1/1 — Cosmic Spirit {Virus} Unit
card('Celestial Fluxmorph', {
  abilities: [{
    type: 'triggered', events: ['spawned'], self: true,
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: When I spawn or become modded, put a +1/+1 counter on each of your units. */ } },
  }],
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'When I despawn, remove all counters from your units.',
    effect: { run: (g, ctx) => { /* TODO: When I despawn, remove all counters from your units. */ } },
  }],
});
// TODO(review): could not guess events for augment text "When I despawn, remove all counters from your unit…"

// ── per-card test stub ────────────────────────────────────────────
test('Celestial Fluxmorph: TODO — When I spawn or become modded, put a +1/+1 counter on each o', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I spawn or become modded, put a +1/+1 counter on each of your units. {/n}[Augment] When I despawn, remove all counters from your units.
  assert.fail('unwritten per-card test — definition of done');
});
