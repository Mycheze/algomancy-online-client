// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Enemies gain +2/+2."
// ee/5 10/15 — Primordial Rock Beast {Virus} Unit
card('Towering Colossus', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Enemies gain +2/+2.',
    effect: { run: (g, ctx) => { /* TODO: Enemies gain +2/+2. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Enemies gain +2/+2.…"

// ── per-card test stub ────────────────────────────────────────────
test('Towering Colossus: TODO — [Augment] Enemies gain +2/+2.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Enemies gain +2/+2.
  assert.fail('unwritten per-card test — definition of done');
});
