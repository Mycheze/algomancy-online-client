// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Double the /[power {i1}or defense] of target unit until regroup."
// g/2 5/5 — {Battle} Druid Spell
card('Burgeon', {
  spellEffect: {
    // TODO: Double the /[power {i1}or defense] of target unit until regroup.
    targets: { what: 'unit', prompt: 'Burgeon: Double the /[power {i1}or defense] of target unit until regroup.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Burgeon: TODO — [Switch1] Double the /[power {i1}or defense] of target unit ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Double the /[power {i1}or defense] of target unit until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
