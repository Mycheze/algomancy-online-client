// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] {Inverted} Ancient Anima {Virus} Unit"
// e/2 2/3 — [Augment] {Inverted} Ancient Anima {Virus} Unit
// type-line [Augment]: augmenting grants {Inverted} — printed.augmentAttrs handles it, no behavior needed
card('Reality Bender', {});

// ── per-card test stub ────────────────────────────────────────────
test('Reality Bender: TODO — Inverted 2/3', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: (vanilla)
  assert.fail('unwritten per-card test — definition of done');
});
