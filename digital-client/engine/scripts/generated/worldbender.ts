// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Skip your draft step. When you do, draw a card. You also lose 3 life if playing a constructed format."
// mm/2 2/2 — {Feeble} Cosmic Robot Unit
card('Worldbender', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: Skip your draft step. When you do, draw a card. You also lose 3 life if playing a constructed format. */ } },
  }],
});
// TODO(review): could not guess events for "Skip your draft step. When you do, draw a card. Yo…" — fill them in

// ── per-card test stub ────────────────────────────────────────────
test('Worldbender: TODO — Skip your draft step. When you do, draw a card. You also los', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Skip your draft step. When you do, draw a card. You also lose 3 life if playing a constructed format.
  assert.fail('unwritten per-card test — definition of done');
});
