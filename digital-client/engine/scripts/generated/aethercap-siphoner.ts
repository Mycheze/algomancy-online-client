// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "I spawn with three -1/-1 counters on me. {/n}[Augment] Whenever you play a nontoken spell, you may move a counter from me onto another target unit."
// gb/3 4/4 — Cosmic Fungus Blight Unit
card('Aethercap Siphoner', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: I spawn with three -1/-1 counters on me. */ } },
  }],
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['spellPlayed'],
    label: 'Whenever you play a nontoken spell, you may move a counter f',
    effect: { run: (g, ctx) => { /* TODO: Whenever you play a nontoken spell, you may move a counter from me onto another target unit. */ } },
  }],
});
// TODO(review): could not guess events for "I spawn with three -1/-1 counters on me.…" — fill them in

// ── per-card test stub ────────────────────────────────────────────
test('Aethercap Siphoner: TODO — I spawn with three -1/-1 counters on me. [Augment] Whenever ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: I spawn with three -1/-1 counters on me. {/n}[Augment] Whenever you play a nontoken spell, you may move a counter from me onto another target unit.
  assert.fail('unwritten per-card test — definition of done');
});
