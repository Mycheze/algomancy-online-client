// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Negate target effect. Its controller gains control of me."
// gg/2 3/1 — {Battle} Arcane Fungus Spell Unit
card('Hush Mush', {
  spellEffect: {
    // TODO: Negate target effect. Its controller gains control of me.
    targets: { what: 'unit', prompt: 'Hush Mush: Negate target effect. Its controller gains control of me.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Hush Mush: TODO — Negate target effect. Its controller gains control of me.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Negate target effect. Its controller gains control of me.
  assert.fail('unwritten per-card test — definition of done');
});
