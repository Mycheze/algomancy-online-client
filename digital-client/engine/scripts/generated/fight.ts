// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Target ally and another target unit fight. {i}(They deal damage to each other equal to their power.)"
// e/1 2/3 — {Battle} Bedlam Spell
card('Fight', {
  spellEffect: {
    // TODO: Target ally and another target unit fight.
    targets: { what: 'unit', prompt: 'Fight: Target ally and another target unit fight.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Fight: TODO — [Switch1] Target ally and another target unit fight.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Target ally and another target unit fight. {i}(They deal damage to each other equal to their power.)
  assert.fail('unwritten per-card test — definition of done');
});
