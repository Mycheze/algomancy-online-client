// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[once] When another ally with greater defense than power spawns, draw a card."
// e/3 1/3 — Bird Insect Oracle Unit
card('Nectar Ridge Oracle', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: [once] When another ally with greater defense than power spawns, draw a card. */ } },
  }],
});
// TODO(review): could not guess events for "[once] When another ally with greater defense than…" — fill them in

// ── per-card test stub ────────────────────────────────────────────
test('Nectar Ridge Oracle: TODO — [once] When another ally with greater defense than power spa', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [once] When another ally with greater defense than power spawns, draw a card.
  assert.fail('unwritten per-card test — definition of done');
});
