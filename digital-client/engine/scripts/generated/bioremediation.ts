// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Target player reveals their hand. You choose {/n}a card from it and put it into your hand."
// ggg/4 2/2 — {Battle} Fungus Spell
card('Bioremediation', {
  spellEffect: {
    // TODO: Target player reveals their hand. You choose a card from it and put it into your hand.
    targets: { what: 'unit', prompt: 'Bioremediation: Target player reveals their hand. You choose a card from it and put it into your hand.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Bioremediation: TODO — Target player reveals their hand. You choose a card from it ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Target player reveals their hand. You choose {/n}a card from it and put it into your hand.
  assert.fail('unwritten per-card test — definition of done');
});
