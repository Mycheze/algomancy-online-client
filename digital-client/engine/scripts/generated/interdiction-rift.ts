// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Target opponent negates an effect they control."
// mm/2 4/3 — {Battle}AI Cosmic Spell
card('Interdiction Rift', {
  spellEffect: {
    // TODO: Target opponent negates an effect they control.
    targets: { what: 'unit', prompt: 'Interdiction Rift: Target opponent negates an effect they control.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Interdiction Rift: TODO — Target opponent negates an effect they control.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Target opponent negates an effect they control.
  assert.fail('unwritten per-card test — definition of done');
});
