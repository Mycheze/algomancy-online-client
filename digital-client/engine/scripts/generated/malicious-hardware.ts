// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever one of your units dies, each opponent sacrifices a unit."
// mmr/5 5/6 — Robot Insect Horror Unit
card('Malicious Hardware', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever one of your units dies, each opponent sacrifices a ',
    effect: { run: (g, ctx) => { /* TODO: Whenever one of your units dies, each opponent sacrifices a unit. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever one of your units dies, each opponent sac…"

// ── per-card test stub ────────────────────────────────────────────
test('Malicious Hardware: TODO — [Augment] Whenever one of your units dies, each opponent sac', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever one of your units dies, each opponent sacrifices a unit.
  assert.fail('unwritten per-card test — definition of done');
});
