// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I activate, if you have at least [e][e][e], create a Shard. {i}(It spawns dormant.)"
// e/0 2/0 — [e] Earth Resource
card('Earth Resource', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: When I activate, if you have at least [e][e][e], create a Shard. */ } },
  }],
});
// TODO(review): could not guess events for "When I activate, if you have at least [e][e][e], c…" — fill them in

// ── per-card test stub ────────────────────────────────────────────
test('Earth Resource: TODO — When I activate, if you have at least [e][e][e], create a Sh', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I activate, if you have at least [e][e][e], create a Shard. {i}(It spawns dormant.)
  assert.fail('unwritten per-card test — definition of done');
});
