// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] After combat, delete all tokens."
// gm/2 2/2 — Alien Fungus Unit
card('Ominous Growth', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['afterCombat'],
    label: 'After combat, delete all tokens.',
    effect: { run: (g, ctx) => { /* TODO: After combat, delete all tokens. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Ominous Growth: TODO — [Augment] After combat, delete all tokens.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] After combat, delete all tokens.
  assert.fail('unwritten per-card test — definition of done');
});
