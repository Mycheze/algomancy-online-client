// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] After the blocking step, I deal 1 damage to each opponent for each blocked column."
// e/2 1/3 — Porcupine Beast Unit
card('Roving Quillback', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'After the blocking step, I deal 1 damage to each opponent fo',
    effect: { run: (g, ctx) => { /* TODO: After the blocking step, I deal 1 damage to each opponent for each blocked column. */ } },
  }],
});
// TODO(review): could not guess events for augment text "After the blocking step, I deal 1 damage to each o…"

// ── per-card test stub ────────────────────────────────────────────
test('Roving Quillback: TODO — [Augment] After the blocking step, I deal 1 damage to each o', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] After the blocking step, I deal 1 damage to each opponent for each blocked column.
  assert.fail('unwritten per-card test — definition of done');
});
