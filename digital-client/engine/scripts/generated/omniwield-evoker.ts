// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[three]: [Switch] Put a +1/+1 counter on me."
// mmm/3 2/1 — Cosmic Robot Unit
card('Omniwield Evoker', {
  abilities: [{
    type: 'triggered', events: [], graftCause: true,
    label: 'Put a +1/+1 counter on me.',
    // TODO trigger clause: "[three]:" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Put a +1/+1 counter on me. */ } },
  }],
  graftEffect: { bounded: false, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): could not guess events for "[three]:" — fill them in
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Omniwield Evoker: TODO — [three]: [Switch] Put a +1/+1 counter on me.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [three]: [Switch] Put a +1/+1 counter on me.
  assert.fail('unwritten per-card test — definition of done');
});
