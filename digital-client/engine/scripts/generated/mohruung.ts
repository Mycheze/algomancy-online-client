// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I become targeted, [Switch1] Create a Crystal 2."
// e/4 1/5 — Cosmic Rock Unit
card('Mohruung', {
  abilities: [{
    type: 'triggered', events: ['targeted'], bounded: true, graftCause: true,
    label: 'Create a Crystal 2.',
    // TODO trigger clause: "When I become targeted" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Create a Crystal 2. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Mohruung: TODO — When I become targeted, [Switch1] Create a Crystal 2.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I become targeted, [Switch1] Create a Crystal 2.
  assert.fail('unwritten per-card test — definition of done');
});
