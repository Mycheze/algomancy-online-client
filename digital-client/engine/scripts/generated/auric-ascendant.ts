// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[once] [one], Recall another ally: I gain {g}flying and +2/+0 until regroup. {i}(Only flying units can block flying units.)"
// bm/2 2/1 — Mystic Avatar Unit
card('Auric Ascendant', {
  abilities: [{
    type: 'activated', cost: { /* TODO: "[once] [one], Recall another ally" */ },
    label: 'I gain {g}flying and +2/+0 until regroup.',
    effect: { run: (g, ctx) => { /* TODO: I gain {g}flying and +2/+0 until regroup. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Auric Ascendant: TODO — [once] [one], Recall another ally: I gain {g}flying and +2/+', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [once] [one], Recall another ally: I gain {g}flying and +2/+0 until regroup. {i}(Only flying units can block flying units.)
  assert.fail('unwritten per-card test — definition of done');
});
