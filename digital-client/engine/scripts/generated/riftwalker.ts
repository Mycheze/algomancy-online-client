// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment][once] [one]: Switch my position with another target ally in my formation."
// m/2 4/2 — Cosmic Strider {Virus} Unit
card('Riftwalker', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [], bounded: true,   // [once]
    label: '[one]: Switch my position with another target ally in my for',
    effect: { run: (g, ctx) => { /* TODO: [one]: Switch my position with another target ally in my formation. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[one]: Switch my position with another target ally…"

// ── per-card test stub ────────────────────────────────────────────
test('Riftwalker: TODO — [Augment][once] [one]: Switch my position with another targe', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment][once] [one]: Switch my position with another target ally in my formation.
  assert.fail('unwritten per-card test — definition of done');
});
