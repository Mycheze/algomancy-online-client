// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Each player sacrifices half of their units, rounded up."
// mm/4 2/1 — {Battle} Occult Spell
card('Perish', {
  spellEffect: {
    // TODO: Each player sacrifices half of their units, rounded up.
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Perish: TODO — Each player sacrifices half of their units, rounded up.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Each player sacrifices half of their units, rounded up.
  assert.fail('unwritten per-card test — definition of done');
});
