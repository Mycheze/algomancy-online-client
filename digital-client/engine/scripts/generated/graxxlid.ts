// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment][once] [one]: Negate target effect targeting me. That effect's controller draws a card."
// ee/2 2/3 — Arcane Guardian {Virus} Unit
card('Graxxlid', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [], bounded: true,   // [once]
    label: '[one]: Negate target effect targeting me. That effect\'s con',
    effect: { run: (g, ctx) => { /* TODO: [one]: Negate target effect targeting me. That effect's controller draws a card. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[one]: Negate target effect targeting me. That eff…"

// ── per-card test stub ────────────────────────────────────────────
test('Graxxlid: TODO — [Augment][once] [one]: Negate target effect targeting me. Th', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment][once] [one]: Negate target effect targeting me. That effect's controller draws a card.
  assert.fail('unwritten per-card test — definition of done');
});
