// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Target ally deals damage equal to its defense to another target unit."
// e/2 2/2 — {Battle} Rock Spell
card('Squish', {
  spellEffect: {
    // TODO: Target ally deals damage equal to its defense to another target unit.
    targets: { what: 'unit', prompt: 'Squish: Target ally deals damage equal to its defense to another target unit.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Squish: TODO — [Switch1] Target ally deals damage equal to its defense to a', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Target ally deals damage equal to its defense to another target unit.
  assert.fail('unwritten per-card test — definition of done');
});
