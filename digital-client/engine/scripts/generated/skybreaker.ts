// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Erase me: Negate all spell effects."
// ee/2 2/2 — Arcane Primordial {Virus} Unit
card('Skybreaker', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Erase me: Negate all spell effects.',
    effect: { run: (g, ctx) => { /* TODO: Erase me: Negate all spell effects. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Erase me: Negate all spell effects.…"

// ── per-card test stub ────────────────────────────────────────────
test('Skybreaker: TODO — [Augment] Erase me: Negate all spell effects.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Erase me: Negate all spell effects.
  assert.fail('unwritten per-card test — definition of done');
});
