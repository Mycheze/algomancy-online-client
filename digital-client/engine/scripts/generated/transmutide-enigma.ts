// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever another ally spawns during battle, double its /[power {i1}or defense] until regroup."
// bm/4 5/4 — Jellyfish Oracle Unit
card('Transmutide Enigma', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever another ally spawns during battle, double its /[pow',
    effect: { run: (g, ctx) => { /* TODO: Whenever another ally spawns during battle, double its /[power {i1}or defense] until regroup. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever another ally spawns during battle, double…"

// ── per-card test stub ────────────────────────────────────────────
test('Transmutide Enigma: TODO — [Augment] Whenever another ally spawns during battle, double', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever another ally spawns during battle, double its /[power {i1}or defense] until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
