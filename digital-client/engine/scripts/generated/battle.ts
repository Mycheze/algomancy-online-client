// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Two target units fight. {i}(They deal damage {/n}to each other equal to their power.)"
// ee/4 4/1 — {Battle} Bedlam Spell
card('Battle', {
  spellEffect: {
    // TODO: Two target units fight.
    targets: { what: 'unit', prompt: 'Battle: Two target units fight.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Battle: TODO — Two target units fight.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Two target units fight. {i}(They deal damage {/n}to each other equal to their power.)
  assert.fail('unwritten per-card test — definition of done');
});
