// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment][two]: Create a 1/1 unit."
// gg/6 4/4 — Plant Unit
card('Pack Leader', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: '[two]: Create a 1/1 unit.',
    effect: { run: (g, ctx) => { /* TODO: [two]: Create a 1/1 unit. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[two]: Create a 1/1 unit.…"

// ── per-card test stub ────────────────────────────────────────────
test('Pack Leader: TODO — [Augment][two]: Create a 1/1 unit.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment][two]: Create a 1/1 unit.
  assert.fail('unwritten per-card test — definition of done');
});
