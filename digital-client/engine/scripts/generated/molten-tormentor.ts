// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever I survive damage, each oppo- nent sacrifices that many units."
// err/7 7/6 — Infernal Rock Elemental Unit
card('Molten Tormentor', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever I survive damage, each oppo- nent sacrifices that m',
    effect: { run: (g, ctx) => { /* TODO: Whenever I survive damage, each oppo- nent sacrifices that many units. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever I survive damage, each oppo- nent sacrifi…"

// ── per-card test stub ────────────────────────────────────────────
test('Molten Tormentor: TODO — [Augment] Whenever I survive damage, each oppo- nent sacrifi', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever I survive damage, each oppo- nent sacrifices that many units.
  assert.fail('unwritten per-card test — definition of done');
});
