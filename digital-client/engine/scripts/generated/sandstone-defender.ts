// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Your other units gain +0/+2."
// e/2 0/3 — Anima Guardian Unit
card('Sandstone Defender', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Your other units gain +0/+2.',
    effect: { run: (g, ctx) => { /* TODO: Your other units gain +0/+2. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Your other units gain +0/+2.…"

// ── per-card test stub ────────────────────────────────────────────
test('Sandstone Defender: TODO — [Augment] Your other units gain +0/+2.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Your other units gain +0/+2.
  assert.fail('unwritten per-card test — definition of done');
});
