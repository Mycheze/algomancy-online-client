// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I spawn, create two 1/1 units. {/n}[Augment] When I despawn, delete all token allies."
// gg/2 2/3 — Fungus Parasite {Virus} Unit
card('Pathogenic Enclave', {
  abilities: [{
    type: 'triggered', events: ['spawned'], self: true,
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: When I spawn, create two 1/1 units. */ } },
  }],
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'When I despawn, delete all token allies.',
    effect: { run: (g, ctx) => { /* TODO: When I despawn, delete all token allies. */ } },
  }],
});
// TODO(review): could not guess events for augment text "When I despawn, delete all token allies.…"

// ── per-card test stub ────────────────────────────────────────────
test('Pathogenic Enclave: TODO — When I spawn, create two 1/1 units. [Augment] When I despawn', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I spawn, create two 1/1 units. {/n}[Augment] When I despawn, delete all token allies.
  assert.fail('unwritten per-card test — definition of done');
});
