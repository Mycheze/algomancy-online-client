// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Sacrifice me and another ally: Delete all units with cost equal to the total number of counters on us."
// m/2 2/2 — Robot Spirit Unit
card('Deformant', {
  abilities: [{
    type: 'activated', cost: { /* TODO: "Sacrifice me and another ally" */ },
    label: 'Delete all units with cost equal to the total number of coun',
    effect: { run: (g, ctx) => { /* TODO: Delete all units with cost equal to the total number of counters on us. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Deformant: TODO — Sacrifice me and another ally: Delete all units with cost eq', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Sacrifice me and another ally: Delete all units with cost equal to the total number of counters on us.
  assert.fail('unwritten per-card test — definition of done');
});
