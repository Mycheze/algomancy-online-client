// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When a player loses life during battle, {/n}[Switch1] Draw a card. {i}(Damage causes life loss.)"
// rb/5 3/3 — Cosmic Elemental Unit
card('Astral Painseeker', {
  abilities: [{
    type: 'triggered', events: [], bounded: true, graftCause: true,
    label: 'Draw a card.',
    // TODO trigger clause: "When a player loses life during battle" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Draw a card. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): could not guess events for "When a player loses life during battle" — fill them in
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Astral Painseeker: TODO — When a player loses life during battle, [Switch1] Draw a car', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When a player loses life during battle, {/n}[Switch1] Draw a card. {i}(Damage causes life loss.)
  assert.fail('unwritten per-card test — definition of done');
});
