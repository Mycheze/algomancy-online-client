// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] [three]: Create a Robot 2."
// mm/4 2/4 — Robot Unit
card('Living Forge', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: '[three]: Create a Robot 2.',
    effect: { run: (g, ctx) => { /* TODO: [three]: Create a Robot 2. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[three]: Create a Robot 2.…"

// ── per-card test stub ────────────────────────────────────────────
test('Living Forge: TODO — [Augment] [three]: Create a Robot 2.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] [three]: Create a Robot 2.
  assert.fail('unwritten per-card test — definition of done');
});
