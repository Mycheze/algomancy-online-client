// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Each player sacrifices two units. If four or more units were sacrificed this way, repeat this."
// rm/4 5/3 — {Battle} Bedlam Maelstrom Spell
card('Maw of Damnation', {
  spellEffect: {
    // TODO: Each player sacrifices two units. If four or more units were sacrificed this way, repeat this.
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Maw of Damnation: TODO — Each player sacrifices two units. If four or more units were', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Each player sacrifices two units. If four or more units were sacrificed this way, repeat this.
  assert.fail('unwritten per-card test — definition of done');
});
