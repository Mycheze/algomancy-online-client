// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] When I attack or block, create a Robot 2 in my formation."
// mm/3 1/2 — Hooba Robot Unit
card('Hooba-Bot', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['attacked', 'blocked'], self: true,
    label: 'When I attack or block, create a Robot 2 in my formation.',
    effect: { run: (g, ctx) => { /* TODO: When I attack or block, create a Robot 2 in my formation. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Hooba-Bot: TODO — [Augment] When I attack or block, create a Robot 2 in my for', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] When I attack or block, create a Robot 2 in my formation.
  assert.fail('unwritten per-card test — definition of done');
});
