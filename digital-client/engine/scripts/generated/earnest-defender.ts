// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever an ally becomes the target of an enemy spell, create a 1/1 unit."
// g/3 1/3 — {Haste} Flower Guardian Unit
card('Earnest Defender', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['targeted'],
    label: 'Whenever an ally becomes the target of an enemy spell, creat',
    effect: { run: (g, ctx) => { /* TODO: Whenever an ally becomes the target of an enemy spell, create a 1/1 unit. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Earnest Defender: TODO — [Augment] Whenever an ally becomes the target of an enemy sp', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever an ally becomes the target of an enemy spell, create a 1/1 unit.
  assert.fail('unwritten per-card test — definition of done');
});
