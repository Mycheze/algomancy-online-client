// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Your other units gain +2/+2 and lose all attributes and abilities."
// mm/4 4/3 — Alien {Virus} Unit
card('Transmogrifant', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Your other units gain +2/+2 and lose all attributes and abil',
    effect: { run: (g, ctx) => { /* TODO: Your other units gain +2/+2 and lose all attributes and abilities. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Your other units gain +2/+2 and lose all attribute…"

// ── per-card test stub ────────────────────────────────────────────
test('Transmogrifant: TODO — [Augment] Your other units gain +2/+2 and lose all attribute', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Your other units gain +2/+2 and lose all attributes and abilities.
  assert.fail('unwritten per-card test — definition of done');
});
