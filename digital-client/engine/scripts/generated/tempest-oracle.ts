// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I despawn, you may pay [one] to draw a card and lose 1 life."
// br/1 2/1 — Infernal Sprite Oracle Unit
card('Tempest Oracle', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: When I despawn, you may pay [one] to draw a card and lose 1 life. */ } },
  }],
});
// TODO(review): could not guess events for "When I despawn, you may pay [one] to draw a card a…" — fill them in

// ── per-card test stub ────────────────────────────────────────────
test('Tempest Oracle: TODO — When I despawn, you may pay [one] to draw a card and lose 1 ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I despawn, you may pay [one] to draw a card and lose 1 life.
  assert.fail('unwritten per-card test — definition of done');
});
