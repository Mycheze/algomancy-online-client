// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] [x]: I become base X/X until regroup."
// m/2 2/2 — {Haste} Cosmic Alien Unit
card('Celestial Shifter', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: '[x]: I become base X/X until regroup.',
    effect: { run: (g, ctx) => { /* TODO: [x]: I become base X/X until regroup. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[x]: I become base X/X until regroup.…"

// ── per-card test stub ────────────────────────────────────────────
test('Celestial Shifter: TODO — [Augment] [x]: I become base X/X until regroup.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] [x]: I become base X/X until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
