// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Switch the power and defense of target unit until regroup."
// mm/2 1/1 — {Battle} {Reaping} Occult Technology Spell
card('Invasive Reassignment', {
  spellEffect: {
    // TODO: Switch the power and defense of target unit until regroup.
    targets: { what: 'unit', prompt: 'Invasive Reassignment: Switch the power and defense of target unit until regroup.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Invasive Reassignment: TODO — Switch the power and defense of target unit until regroup.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Switch the power and defense of target unit until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
