// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I am dealt damage, [Switch1] Put a +1/+1 counter on me."
// ee/2 0/4 — {Sluggish} Rock Beast Unit
card('Plodding Pebble', {
  abilities: [{
    type: 'triggered', events: ['damage'], self: true, bounded: true, graftCause: true,
    label: 'Put a +1/+1 counter on me.',
    // TODO trigger clause: "When I am dealt damage" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Put a +1/+1 counter on me. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Plodding Pebble: TODO — When I am dealt damage, [Switch1] Put a +1/+1 counter on me.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I am dealt damage, [Switch1] Put a +1/+1 counter on me.
  assert.fail('unwritten per-card test — definition of done');
});
