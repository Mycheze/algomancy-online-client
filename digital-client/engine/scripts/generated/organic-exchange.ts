// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Exchange control of two target units and swap their positions."
// gg/3 4/5 — {Battle} Fungus Spell
card('Organic Exchange', {
  spellEffect: {
    // TODO: Exchange control of two target units and swap their positions.
    targets: { what: 'unit', prompt: 'Organic Exchange: Exchange control of two target units and swap their positions.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Organic Exchange: TODO — Exchange control of two target units and swap their position', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Exchange control of two target units and swap their positions.
  assert.fail('unwritten per-card test — definition of done');
});
