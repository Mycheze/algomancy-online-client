// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Sacrifice another unit: Your units gain +1/+1 until regroup."
// rg/4 0/4 — Ancient Tree Structure Unit
card('Hearthwood Ancient', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Sacrifice another unit: Your units gain +1/+1 until regroup.',
    effect: { run: (g, ctx) => { /* TODO: Sacrifice another unit: Your units gain +1/+1 until regroup. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Sacrifice another unit: Your units gain +1/+1 unti…"

// ── per-card test stub ────────────────────────────────────────────
test('Hearthwood Ancient: TODO — [Augment] Sacrifice another unit: Your units gain +1/+1 unti', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Sacrifice another unit: Your units gain +1/+1 until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
