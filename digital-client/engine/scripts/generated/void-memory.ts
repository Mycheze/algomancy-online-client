// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Each opponent discards a /[unit {i1}or spell] if {/n}able. Otherwise, they reveal their hand."
// m/2 0/1 — {Battle} Technology Spell
card('Void Memory', {
  spellEffect: {
    // TODO: Each opponent discards a /[unit {i1}or spell] if able. Otherwise, they reveal their hand.
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Void Memory: TODO — [Switch1] Each opponent discards a /[unit {i1}or spell] if a', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Each opponent discards a /[unit {i1}or spell] if {/n}able. Otherwise, they reveal their hand.
  assert.fail('unwritten per-card test — definition of done');
});
