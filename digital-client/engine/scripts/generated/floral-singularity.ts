// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "/[Create X 1/1 units {i1}or your units become base X/X until regroup]."
// ggm/X 2/2 — Cosmic Flower Spell
card('Floral Singularity', {
  spellEffect: {
    // TODO: /[Create X 1/1 units {i1}or your units become base X/X until regroup].
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Floral Singularity: TODO — /[Create X 1/1 units {i1}or your units become base X/X until', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: /[Create X 1/1 units {i1}or your units become base X/X until regroup].
  assert.fail('unwritten per-card test — definition of done');
});
