// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment][once] When I am dealt damage, create that many 1/1 units."
// gg/3 0/4 — Alien Insect Plant Unit
card('Jollyglop', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['damage'], self: true, bounded: true,   // [once]
    label: 'When I am dealt damage, create that many 1/1 units.',
    effect: { run: (g, ctx) => { /* TODO: When I am dealt damage, create that many 1/1 units. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Jollyglop: TODO — [Augment][once] When I am dealt damage, create that many 1/1', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment][once] When I am dealt damage, create that many 1/1 units.
  assert.fail('unwritten per-card test — definition of done');
});
