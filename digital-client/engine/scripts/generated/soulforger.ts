// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever one of your nontoken units dies, create a Fireball 1."
// rrm/2 2/2 — Demon Robot Unit
card('Soulforger', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever one of your nontoken units dies, create a Fireball ',
    effect: { run: (g, ctx) => { /* TODO: Whenever one of your nontoken units dies, create a Fireball 1. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever one of your nontoken units dies, create a…"

// ── per-card test stub ────────────────────────────────────────────
test('Soulforger: TODO — [Augment] Whenever one of your nontoken units dies, create a', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever one of your nontoken units dies, create a Fireball 1.
  assert.fail('unwritten per-card test — definition of done');
});
