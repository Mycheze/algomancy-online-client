// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Gain control of target unit with cost [x] or less unless its controller pays [x]."
// gm/X 2/2 — {Battle} Alien Spell
card('Abduct', {
  spellEffect: {
    // TODO: Gain control of target unit with cost [x] or less unless its controller pays [x].
    targets: { what: 'unit', prompt: 'Abduct: Gain control of target unit with cost [x] or less unless its controller pays [x].' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Abduct: TODO — Gain control of target unit with cost [x] or less unless its', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Gain control of target unit with cost [x] or less unless its controller pays [x].
  assert.fail('unwritten per-card test — definition of done');
});
