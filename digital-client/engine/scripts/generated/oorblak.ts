// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] If combat damage would be dealt to you, that damage is dealt to me instead."
// eee/4 2/4 — {Unstable} Luminary Strider {Virus} Unit
card('Oorblak', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'If combat damage would be dealt to you, that damage is dealt',
    effect: { run: (g, ctx) => { /* TODO: If combat damage would be dealt to you, that damage is dealt to me instead. */ } },
  }],
});
// TODO(review): could not guess events for augment text "If combat damage would be dealt to you, that damag…"

// ── per-card test stub ────────────────────────────────────────────
test('Oorblak: TODO — [Augment] If combat damage would be dealt to you, that damag', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] If combat damage would be dealt to you, that damage is dealt to me instead.
  assert.fail('unwritten per-card test — definition of done');
});
