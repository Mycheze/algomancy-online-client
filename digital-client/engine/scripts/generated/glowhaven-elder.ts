// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Your other units gain +1/+1."
// gg/3 3/3 — Mystic Tree {Virus} Unit
card('Glowhaven Elder', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Your other units gain +1/+1.',
    effect: { run: (g, ctx) => { /* TODO: Your other units gain +1/+1. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Your other units gain +1/+1.…"

// ── per-card test stub ────────────────────────────────────────────
test('Glowhaven Elder: TODO — [Augment] Your other units gain +1/+1.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Your other units gain +1/+1.
  assert.fail('unwritten per-card test — definition of done');
});
