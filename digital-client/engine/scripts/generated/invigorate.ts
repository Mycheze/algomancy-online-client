// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Target unit gains +0/+1 until regroup. Draw a card."
// g/1 1/3 — {Battle} Mystic Druid Spell
card('Invigorate', {
  spellEffect: {
    // TODO: Target unit gains +0/+1 until regroup. Draw a card.
    targets: { what: 'unit', prompt: 'Invigorate: Target unit gains +0/+1 until regroup. Draw a card.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Invigorate: TODO — Target unit gains +0/+1 until regroup. Draw a card.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Target unit gains +0/+1 until regroup. Draw a card.
  assert.fail('unwritten per-card test — definition of done');
});
