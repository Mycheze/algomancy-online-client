// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Delete target unit with base power 2 or less."
// m/2 2/1 — {Battle} Occult Technology Spell
card('Unmake', {
  spellEffect: {
    // TODO: Delete target unit with base power 2 or less.
    targets: { what: 'unit', prompt: 'Unmake: Delete target unit with base power 2 or less.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Unmake: TODO — Delete target unit with base power 2 or less.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Delete target unit with base power 2 or less.
  assert.fail('unwritten per-card test — definition of done');
});
