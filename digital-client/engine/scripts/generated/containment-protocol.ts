// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Negate all activated and triggered effects."
// mm/3 4/1 — {Battle} Technology Spell
card('Containment Protocol', {
  spellEffect: {
    // TODO: Negate all activated and triggered effects.
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Containment Protocol: TODO — Negate all activated and triggered effects.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Negate all activated and triggered effects.
  assert.fail('unwritten per-card test — definition of done');
});
