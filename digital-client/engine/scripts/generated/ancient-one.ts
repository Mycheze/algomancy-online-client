// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] I have all abilities of adjacent allies. {/n}{i}(This includes modded abilities.)"
// mm/2 1/1 — Ancient Mimic Unit
card('Ancient One', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'I have all abilities of adjacent allies.',
    effect: { run: (g, ctx) => { /* TODO: I have all abilities of adjacent allies. */ } },
  }],
});
// TODO(review): could not guess events for augment text "I have all abilities of adjacent allies.…"

// ── per-card test stub ────────────────────────────────────────────
test('Ancient One: TODO — [Augment] I have all abilities of adjacent allies.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] I have all abilities of adjacent allies. {/n}{i}(This includes modded abilities.)
  assert.fail('unwritten per-card test — definition of done');
});
