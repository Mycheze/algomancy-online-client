// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] If one or more counters would be put on a unit by an allied source, put that many counters plus one instead."
// mm/2 2/2 — Technology Construct Unit
card('Flux Resonator', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'If one or more counters would be put on a unit by an allied ',
    effect: { run: (g, ctx) => { /* TODO: If one or more counters would be put on a unit by an allied source, put that many counters plus one instead. */ } },
  }],
});
// TODO(review): could not guess events for augment text "If one or more counters would be put on a unit by …"

// ── per-card test stub ────────────────────────────────────────────
test('Flux Resonator: TODO — [Augment] If one or more counters would be put on a unit by ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] If one or more counters would be put on a unit by an allied source, put that many counters plus one instead.
  assert.fail('unwritten per-card test — definition of done');
});
