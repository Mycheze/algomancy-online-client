// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Negate target effect that targets an allied effect, player or unit."
// gg/1 1/5 — {Battle} Druid Spell
card('Boon of Protection', {
  spellEffect: {
    // TODO: Negate target effect that targets an allied effect, player or unit.
    targets: { what: 'unit', prompt: 'Boon of Protection: Negate target effect that targets an allied effect, player or unit.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Boon of Protection: TODO — Negate target effect that targets an allied effect, player o', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Negate target effect that targets an allied effect, player or unit.
  assert.fail('unwritten per-card test — definition of done');
});
