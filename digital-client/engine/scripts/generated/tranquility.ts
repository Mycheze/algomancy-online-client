// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Spells cost [one] more to play during battle."
// ee/3 3/4 — Mystic Structure Unit
card('Tranquility', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Spells cost [one] more to play during battle.',
    effect: { run: (g, ctx) => { /* TODO: Spells cost [one] more to play during battle. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Spells cost [one] more to play during battle.…"

// ── per-card test stub ────────────────────────────────────────────
test('Tranquility: TODO — [Augment] Spells cost [one] more to play during battle.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Spells cost [one] more to play during battle.
  assert.fail('unwritten per-card test — definition of done');
});
