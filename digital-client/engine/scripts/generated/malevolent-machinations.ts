// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "/[Sacrifice X units]: Negate up to X target effects."
// rrm/2 5/3 — {Battle} Occult Technology Spell
card('Malevolent Machinations', {
  spellEffect: {
    // TODO: /[Sacrifice X units]: Negate up to X target effects.
    targets: { what: 'unit', prompt: 'Malevolent Machinations: /[Sacrifice X units]: Negate up to X target effects.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Malevolent Machinations: TODO — /[Sacrifice X units]: Negate up to X target effects.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: /[Sacrifice X units]: Negate up to X target effects.
  assert.fail('unwritten per-card test — definition of done');
});
