// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever I am modded or applied as a mod, put two +1/+1 counters on me."
// m/2 0/1 — Technology Strider {Virus} Unit
card('Evolutionary Experiment', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever I am modded or applied as a mod, put two +1/+1 coun',
    effect: { run: (g, ctx) => { /* TODO: Whenever I am modded or applied as a mod, put two +1/+1 counters on me. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever I am modded or applied as a mod, put two …"

// ── per-card test stub ────────────────────────────────────────────
test('Evolutionary Experiment: TODO — [Augment] Whenever I am modded or applied as a mod, put two ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever I am modded or applied as a mod, put two +1/+1 counters on me.
  assert.fail('unwritten per-card test — definition of done');
});
