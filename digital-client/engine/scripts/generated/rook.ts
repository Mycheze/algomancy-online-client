// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] You may augment cards from hand and bin during battle as if they were [Virus]."
// me/4 4/4 — Polyform Crab Unit
card('Rook', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'You may augment cards from hand and bin during battle as if ',
    effect: { run: (g, ctx) => { /* TODO: You may augment cards from hand and bin during battle as if they were [Virus]. */ } },
  }],
});
// TODO(review): could not guess events for augment text "You may augment cards from hand and bin during bat…"

// ── per-card test stub ────────────────────────────────────────────
test('Rook: TODO — [Augment] You may augment cards from hand and bin during bat', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] You may augment cards from hand and bin during battle as if they were [Virus].
  assert.fail('unwritten per-card test — definition of done');
});
