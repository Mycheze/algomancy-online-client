// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "You may play one unit with no abilities from your bin each deployment. {i}(Attributes are not abilities.)"
// e/2 2/2 — Primordial Occult Unit
card('The Bonesculptor', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: You may play one unit with no abilities from your bin each deployment. */ } },
  }],
});
// TODO(review): could not guess events for "You may play one unit with no abilities from your …" — fill them in

// ── per-card test stub ────────────────────────────────────────────
test('The Bonesculptor: TODO — You may play one unit with no abilities from your bin each d', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: You may play one unit with no abilities from your bin each deployment. {i}(Attributes are not abilities.)
  assert.fail('unwritten per-card test — definition of done');
});
