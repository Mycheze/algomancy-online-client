// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] After combat, sacrifice another unit."
// er/1 4/4 — Occult Anima {Virus} Unit
card('Unstable Form', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['afterCombat'],
    label: 'After combat, sacrifice another unit.',
    effect: { run: (g, ctx) => { /* TODO: After combat, sacrifice another unit. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Unstable Form: TODO — [Augment] After combat, sacrifice another unit.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] After combat, sacrifice another unit.
  assert.fail('unwritten per-card test — definition of done');
});
