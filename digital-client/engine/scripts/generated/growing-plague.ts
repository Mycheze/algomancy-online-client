// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I spawn, draw a card. {/n}[Augment] When I despawn, each other player draws two cards."
// g/2 2/1 — Alien Slime Parasite {Virus} Unit
card('Growing Plague', {
  abilities: [{
    type: 'triggered', events: ['spawned'], self: true,
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: When I spawn, draw a card. */ } },
  }],
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'When I despawn, each other player draws two cards.',
    effect: { run: (g, ctx) => { /* TODO: When I despawn, each other player draws two cards. */ } },
  }],
});
// TODO(review): could not guess events for augment text "When I despawn, each other player draws two cards.…"

// ── per-card test stub ────────────────────────────────────────────
test('Growing Plague: TODO — When I spawn, draw a card. [Augment] When I despawn, each ot', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I spawn, draw a card. {/n}[Augment] When I despawn, each other player draws two cards.
  assert.fail('unwritten per-card test — definition of done');
});
