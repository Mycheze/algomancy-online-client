// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "I spawn with two +1/+1 counters. {/n} [Augment] When I despawn, you may move my counters onto target unit."
// m/2 0/0 — Robot Unit
card('Powerforge Synergist', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: I spawn with two +1/+1 counters. */ } },
  }],
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'When I despawn, you may move my counters onto target unit.',
    effect: { run: (g, ctx) => { /* TODO: When I despawn, you may move my counters onto target unit. */ } },
  }],
});
// TODO(review): could not guess events for "I spawn with two +1/+1 counters.…" — fill them in
// TODO(review): could not guess events for augment text "When I despawn, you may move my counters onto targ…"

// ── per-card test stub ────────────────────────────────────────────
test('Powerforge Synergist: TODO — I spawn with two +1/+1 counters. [Augment] When I despawn, y', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: I spawn with two +1/+1 counters. {/n} [Augment] When I despawn, you may move my counters onto target unit.
  assert.fail('unwritten per-card test — definition of done');
});
