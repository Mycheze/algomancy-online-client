// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When you create a token, [Switch1] Target ally gains +3/+3 until regroup."
// g/1 2/1 — Fungus Unit
card('Mycelial Mentor', {
  abilities: [{
    type: 'triggered', events: [], bounded: true, graftCause: true,
    label: 'Target ally gains +3/+3 until regroup.',
    // TODO trigger clause: "When you create a token" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Target ally gains +3/+3 until regroup. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): could not guess events for "When you create a token" — fill them in
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Mycelial Mentor: TODO — When you create a token, [Switch1] Target ally gains +3/+3 u', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When you create a token, [Switch1] Target ally gains +3/+3 until regroup.
  assert.fail('unwritten per-card test — definition of done');
});
