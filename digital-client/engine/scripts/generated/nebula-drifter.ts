// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] {Flying} Alien Cloud {Virus} Unit"
// m/1 1/1 — [Augment] {Flying} Alien Cloud {Virus} Unit
// type-line [Augment]: augmenting grants {Flying} — printed.augmentAttrs handles it, no behavior needed
card('Nebula Drifter', {});

// ── per-card test stub ────────────────────────────────────────────
test('Nebula Drifter: TODO — Flying 1/1', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: (vanilla)
  assert.fail('unwritten per-card test — definition of done');
});
