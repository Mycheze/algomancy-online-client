// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "I deal 3 damage to any target."
// ee/3 2/4 — {Battle} {Reaping} Primordial Spell
card('Seismomancy', {
  spellEffect: {
    // TODO: I deal 3 damage to any target.
    targets: { what: 'unit', prompt: 'Seismomancy: I deal 3 damage to any target.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Seismomancy: TODO — I deal 3 damage to any target.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: I deal 3 damage to any target.
  assert.fail('unwritten per-card test — definition of done');
});
