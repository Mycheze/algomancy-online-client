// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Recall X target nontoken allies. Then each player sacrifices a unit and you lose 1 life for each ally recalled this way."
// br/X 1/3 — {Battle} Elemental Spell
card('Torrential Reclamation', {
  spellEffect: {
    // TODO: Recall X target nontoken allies. Then each player sacrifices a unit and you lose 1 life for each ally recalled this way.
    targets: { what: 'unit', prompt: 'Torrential Reclamation: Recall X target nontoken allies. Then each player sacrifices a unit and you lose 1 life for each ally recalled this way.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Torrential Reclamation: TODO — Recall X target nontoken allies. Then each player sacrifices', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Recall X target nontoken allies. Then each player sacrifices a unit and you lose 1 life for each ally recalled this way.
  assert.fail('unwritten per-card test — definition of done');
});
