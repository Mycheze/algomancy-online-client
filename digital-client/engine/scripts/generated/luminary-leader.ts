// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I attack in a formation of four or more units, [Switch1] Draw a card."
// g/3 3/3 — Plant Luminary Unit
card('Luminary Leader', {
  abilities: [{
    type: 'triggered', events: ['attacked'], self: true, bounded: true, graftCause: true,
    label: 'Draw a card.',
    // TODO trigger clause: "When I attack in a formation of four or more units" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Draw a card. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Luminary Leader: TODO — When I attack in a formation of four or more units, [Switch1', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I attack in a formation of four or more units, [Switch1] Draw a card.
  assert.fail('unwritten per-card test — definition of done');
});
