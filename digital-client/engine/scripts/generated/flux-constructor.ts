// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever one of your units with one or more counters on it dies, you may put those counters onto another target unit."
// m/3 3/3 — Alien Robot Unit
card('Flux Constructor', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever one of your units with one or more counters on it d',
    effect: { run: (g, ctx) => { /* TODO: Whenever one of your units with one or more counters on it dies, you may put those counters onto another target unit. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever one of your units with one or more counte…"

// ── per-card test stub ────────────────────────────────────────────
test('Flux Constructor: TODO — [Augment] Whenever one of your units with one or more counte', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever one of your units with one or more counters on it dies, you may put those counters onto another target unit.
  assert.fail('unwritten per-card test — definition of done');
});
