// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Duplicate each counter on target unit."
// m/2 0/0 — {Battle} Technology Spell
card('Technological Superiority', {
  spellEffect: {
    // TODO: Duplicate each counter on target unit.
    targets: { what: 'unit', prompt: 'Technological Superiority: Duplicate each counter on target unit.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Technological Superiority: TODO — [Switch1] Duplicate each counter on target unit.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Duplicate each counter on target unit.
  assert.fail('unwritten per-card test — definition of done');
});
