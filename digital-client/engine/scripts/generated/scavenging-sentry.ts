// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Sacrifice another unit: Put a +1/+1 counter on me."
// mm/2 2/2 — Occult Scrap Unit
card('Scavenging Sentry', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Sacrifice another unit: Put a +1/+1 counter on me.',
    effect: { run: (g, ctx) => { /* TODO: Sacrifice another unit: Put a +1/+1 counter on me. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Sacrifice another unit: Put a +1/+1 counter on me.…"

// ── per-card test stub ────────────────────────────────────────────
test('Scavenging Sentry: TODO — [Augment] Sacrifice another unit: Put a +1/+1 counter on me.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Sacrifice another unit: Put a +1/+1 counter on me.
  assert.fail('unwritten per-card test — definition of done');
});
