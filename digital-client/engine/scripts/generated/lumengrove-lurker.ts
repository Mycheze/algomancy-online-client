// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "After combat, [Switch1] Recall up to one target unit with cost less than or equal to the number of units in my formation."
// bg/2 3/2 — Mystic Fungus Unit
card('Lumengrove Lurker', {
  abilities: [{
    type: 'triggered', events: ['afterCombat'], bounded: true, graftCause: true,
    label: 'Recall up to one target unit with cost less than or equal to',
    // TODO trigger clause: "After combat" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Recall up to one target unit with cost less than or equal to the number of units in my formation. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Lumengrove Lurker: TODO — After combat, [Switch1] Recall up to one target unit with co', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: After combat, [Switch1] Recall up to one target unit with cost less than or equal to the number of units in my formation.
  assert.fail('unwritten per-card test — definition of done');
});
