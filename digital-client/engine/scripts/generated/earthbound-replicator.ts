// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever a player plays a nonunit spell targeting me, they copy it and may choose new targets for the copy."
// ge/2 1/3 — Primordial Elemental Unit
card('Earthbound Replicator', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['spellPlayed'],
    label: 'Whenever a player plays a nonunit spell targeting me, they c',
    effect: { run: (g, ctx) => { /* TODO: Whenever a player plays a nonunit spell targeting me, they copy it and may choose new targets for the copy. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Earthbound Replicator: TODO — [Augment] Whenever a player plays a nonunit spell targeting ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever a player plays a nonunit spell targeting me, they copy it and may choose new targets for the copy.
  assert.fail('unwritten per-card test — definition of done');
});
