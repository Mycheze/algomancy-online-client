// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] After combat, put a -1/-1 counter on each {/n}unit, then I deal 2 damage to each player."
// rg/4 3/5 — Infernal Fungus Unit
card('Infernal Grovekeeper', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: ['afterCombat'],
    label: 'After combat, put a -1/-1 counter on each unit, then I deal ',
    effect: { run: (g, ctx) => { /* TODO: After combat, put a -1/-1 counter on each unit, then I deal 2 damage to each player. */ } },
  }],
});

// ── per-card test stub ────────────────────────────────────────────
test('Infernal Grovekeeper: TODO — [Augment] After combat, put a -1/-1 counter on each unit, th', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] After combat, put a -1/-1 counter on each {/n}unit, then I deal 2 damage to each player.
  assert.fail('unwritten per-card test — definition of done');
});
