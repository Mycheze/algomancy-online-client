// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Each turn, you may play a unit during the mana step as if it had [Haste]."
// mm/2 2/1 — Robot Horse Unit
card('Dispatch Courier', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Each turn, you may play a unit during the mana step as if it',
    effect: { run: (g, ctx) => { /* TODO: Each turn, you may play a unit during the mana step as if it had [Haste]. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Each turn, you may play a unit during the mana ste…"

// ── per-card test stub ────────────────────────────────────────────
test('Dispatch Courier: TODO — [Augment] Each turn, you may play a unit during the mana ste', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Each turn, you may play a unit during the mana step as if it had [Haste].
  assert.fail('unwritten per-card test — definition of done');
});
