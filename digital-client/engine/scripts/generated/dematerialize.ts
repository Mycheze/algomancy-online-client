// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Negate target effect. Its controller Glimpses 3. {i}(They reveal the top three cards of their deck and cache one. Until end of turn, they may play it as if it was in their hand, ignoring affinity. Recycle the rest.)"
// bm/2 2/1 — {Battle} Cosmic Technology Spell
card('Dematerialize', {
  spellEffect: {
    // TODO: Negate target effect. Its controller Glimpses 3.
    targets: { what: 'unit', prompt: 'Dematerialize: Negate target effect. Its controller Glimpses 3.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Dematerialize: TODO — Negate target effect. Its controller Glimpses 3.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Negate target effect. Its controller Glimpses 3. {i}(They reveal the top three cards of their deck and cache one. Until end of turn, they may play it as if it was in their hand, ignoring affinity. Recycle the rest.)
  assert.fail('unwritten per-card test — definition of done');
});
