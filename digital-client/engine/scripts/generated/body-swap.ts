// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Exchange the base stats of two target units until regroup. {i}(With each other.)"
// m/2 2/1 — {Battle} Arcane Spell
card('Body Swap', {
  spellEffect: {
    // TODO: Exchange the base stats of two target units until regroup.
    targets: { what: 'unit', prompt: 'Body Swap: Exchange the base stats of two target units until regroup.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Body Swap: TODO — [Switch1] Exchange the base stats of two target units until ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Exchange the base stats of two target units until regroup. {i}(With each other.)
  assert.fail('unwritten per-card test — definition of done');
});
