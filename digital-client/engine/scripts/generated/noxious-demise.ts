// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Put a -1/-1 counter on target unit."
// gg/1 1/2 — {Battle} {Reaping} Arcane Blight Spell
card('Noxious Demise', {
  spellEffect: {
    // TODO: Put a -1/-1 counter on target unit.
    targets: { what: 'unit', prompt: 'Noxious Demise: Put a -1/-1 counter on target unit.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Noxious Demise: TODO — Put a -1/-1 counter on target unit.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Put a -1/-1 counter on target unit.
  assert.fail('unwritten per-card test — definition of done');
});
