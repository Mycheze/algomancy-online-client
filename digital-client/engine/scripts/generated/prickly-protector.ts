// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] I gain +1/+1 for each other ally."
// g/1 0/1 — Plant Beast Unit
card('Prickly Protector', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'I gain +1/+1 for each other ally.',
    effect: { run: (g, ctx) => { /* TODO: I gain +1/+1 for each other ally. */ } },
  }],
});
// TODO(review): could not guess events for augment text "I gain +1/+1 for each other ally.…"

// ── per-card test stub ────────────────────────────────────────────
test('Prickly Protector: TODO — [Augment] I gain +1/+1 for each other ally.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] I gain +1/+1 for each other ally.
  assert.fail('unwritten per-card test — definition of done');
});
