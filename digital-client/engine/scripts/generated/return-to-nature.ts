// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Negate all effects. Erase all mods."
// eee/4 2/2 — {Battle} Druid Spell
card('Return to Nature', {
  spellEffect: {
    // TODO: Negate all effects. Erase all mods.
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Return to Nature: TODO — Negate all effects. Erase all mods.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Negate all effects. Erase all mods.
  assert.fail('unwritten per-card test — definition of done');
});
