// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment][once] [one], Erase one of my mods: I deal 2 damage to any target."
// er/2 2/2 —  Slag Beast {Virus} Unit
card('Slag Spewer', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [], bounded: true,   // [once]
    label: '[one], Erase one of my mods: I deal 2 damage to any target.',
    effect: { run: (g, ctx) => { /* TODO: [one], Erase one of my mods: I deal 2 damage to any target. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[one], Erase one of my mods: I deal 2 damage to an…"

// ── per-card test stub ────────────────────────────────────────────
test('Slag Spewer: TODO — [Augment][once] [one], Erase one of my mods: I deal 2 damage', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment][once] [one], Erase one of my mods: I deal 2 damage to any target.
  assert.fail('unwritten per-card test — definition of done');
});
