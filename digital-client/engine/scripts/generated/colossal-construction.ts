// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Create a Robot X, where X is the greatest defense among your units."
// em/3 2/4 — Primordial Technology Spell
card('Colossal Construction', {
  spellEffect: {
    // TODO: Create a Robot X, where X is the greatest defense among your units.
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Colossal Construction: TODO — [Switch1] Create a Robot X, where X is the greatest defense ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Create a Robot X, where X is the greatest defense among your units.
  assert.fail('unwritten per-card test — definition of done');
});
