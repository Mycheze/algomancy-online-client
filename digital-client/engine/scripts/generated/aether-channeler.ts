// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever another nontoken ally spawns, create a Crystal 1."
// eg/2 2/3 — Plant Primordial Unit
card('Aether Channeler', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever another nontoken ally spawns, create a Crystal 1.',
    effect: { run: (g, ctx) => { /* TODO: Whenever another nontoken ally spawns, create a Crystal 1. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever another nontoken ally spawns, create a Cr…"

// ── per-card test stub ────────────────────────────────────────────
test('Aether Channeler: TODO — [Augment] Whenever another nontoken ally spawns, create a Cr', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever another nontoken ally spawns, create a Crystal 1.
  assert.fail('unwritten per-card test — definition of done');
});
