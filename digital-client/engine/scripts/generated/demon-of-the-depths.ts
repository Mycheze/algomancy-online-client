// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever one of your units despawns, {/n}I deal 1 damage to any target."
// br/4 3/3 — Demon Spirit Unit
card('Demon of the Depths', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever one of your units despawns, I deal 1 damage to any ',
    effect: { run: (g, ctx) => { /* TODO: Whenever one of your units despawns, I deal 1 damage to any target. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever one of your units despawns, I deal 1 dama…"

// ── per-card test stub ────────────────────────────────────────────
test('Demon of the Depths: TODO — [Augment] Whenever one of your units despawns, I deal 1 dama', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever one of your units despawns, {/n}I deal 1 damage to any target.
  assert.fail('unwritten per-card test — definition of done');
});
