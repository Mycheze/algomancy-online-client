// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] [x], Sacrifice another nontoken unit: Create a Robot X. X can't be 0."
// mm/2 3/1 — Spirit Construct Unit
card('Instrument of Reassignment', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: '[x], Sacrifice another nontoken unit: Create a Robot X. X ca',
    effect: { run: (g, ctx) => { /* TODO: [x], Sacrifice another nontoken unit: Create a Robot X. X can't be 0. */ } },
  }],
});
// TODO(review): could not guess events for augment text "[x], Sacrifice another nontoken unit: Create a Rob…"

// ── per-card test stub ────────────────────────────────────────────
test('Instrument of Reassignment: TODO — [Augment] [x], Sacrifice another nontoken unit: Create a Rob', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] [x], Sacrifice another nontoken unit: Create a Robot X. X can't be 0.
  assert.fail('unwritten per-card test — definition of done');
});
