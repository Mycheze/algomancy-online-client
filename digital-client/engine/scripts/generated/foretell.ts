// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Switch1] Glimpse 1 {i}(Reveal the top card of the deck {/n}and cache it. Until end of turn, you may play it {/n}as if it was in your hand, ignoring affinity.)"
// m/1 2/2 — {Battle} Cosmic Spell
card('Foretell', {
  spellEffect: {
    // TODO: Glimpse 1
    run: (g, ctx) => { /* TODO */ },
  },
  graftEffect: { bounded: true, effect: /* TODO: share the spellEffect const */ undefined as never },
});
// TODO(review): graftEffect shares the [Switch] effect — hoist it into a const

// ── per-card test stub ────────────────────────────────────────────
test('Foretell: TODO — [Switch1] Glimpse 1', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Switch1] Glimpse 1 {i}(Reveal the top card of the deck {/n}and cache it. Until end of turn, you may play it {/n}as if it was in your hand, ignoring affinity.)
  assert.fail('unwritten per-card test — definition of done');
});
