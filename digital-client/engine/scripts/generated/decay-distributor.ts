// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "[Augment] Whenever I am dealt damage, put that many -1/-1 counters on target unit."
// eg/4 0/7 — {Haste} Primordial Blight Unit
card('Decay Distributor', {
  augmentText: [{   // text-box [Augment]: transfers TEXT only, never attrs
    type: 'triggered', events: [],
    label: 'Whenever I am dealt damage, put that many -1/-1 counters on ',
    effect: { run: (g, ctx) => { /* TODO: Whenever I am dealt damage, put that many -1/-1 counters on target unit. */ } },
  }],
});
// TODO(review): could not guess events for augment text "Whenever I am dealt damage, put that many -1/-1 co…"

// ── per-card test stub ────────────────────────────────────────────
test('Decay Distributor: TODO — [Augment] Whenever I am dealt damage, put that many -1/-1 co', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: [Augment] Whenever I am dealt damage, put that many -1/-1 counters on target unit.
  assert.fail('unwritten per-card test — definition of done');
});
