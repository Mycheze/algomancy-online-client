// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "If you would create a Robot, Poison, Crystal or Fireball, you may instead create a token of any of these types. {i}(With the same X value.)"
// m/3 3/3 — Luminary Unit
card('Cosmic Conspirator', {
  abilities: [{
    type: 'triggered', events: [],
    label: 'TODO',
    effect: { run: (g, ctx) => { /* TODO: If you would create a Robot, Poison, Crystal or Fireball, you may instead create a token of any of these types. */ } },
  }],
});
// TODO(review): could not guess events for "If you would create a Robot, Poison, Crystal or Fi…" — fill them in

// ── per-card test stub ────────────────────────────────────────────
test('Cosmic Conspirator: TODO — If you would create a Robot, Poison, Crystal or Fireball, yo', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: If you would create a Robot, Poison, Crystal or Fireball, you may instead create a token of any of these types. {i}(With the same X value.)
  assert.fail('unwritten per-card test — definition of done');
});
