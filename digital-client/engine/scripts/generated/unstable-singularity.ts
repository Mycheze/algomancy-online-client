// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "When I die, [Switch1] Delete target unit."
// m/3 0/1 — Cosmic Anima Unit
card('Unstable Singularity', {
  abilities: [{
    type: 'triggered', events: ['died'], self: true, bounded: true, graftCause: true,
    label: 'Delete target unit.',
    // TODO trigger clause: "When I die" — add when() if it has a condition (R1: checked at event time)
    effect: { run: (g, ctx) => { /* TODO: Delete target unit. */ } },
  }],
  graftEffect: { bounded: true, effect: /* TODO: share the effect const */ undefined as never },
});
// TODO(review): hoist the effect into a shared const for ability + graftEffect

// ── per-card test stub ────────────────────────────────────────────
test('Unstable Singularity: TODO — When I die, [Switch1] Delete target unit.', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: When I die, [Switch1] Delete target unit.
  assert.fail('unwritten per-card test — definition of done');
});
