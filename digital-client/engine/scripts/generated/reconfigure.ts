// generated by scripts/gen-card.mjs — move the pieces, then DELETE this file

// ── registry.ts skeleton ──────────────────────────────────────────
// "Augment target unit and all of its mods onto another target unit. {i}(The first target must have{/i} [Augment] {i}to be able to be augmented.)"
// em/4 1/2 — {Battle} Primordial Technology Spell
card('Reconfigure', {
  spellEffect: {
    // TODO: Augment target unit and all of its mods onto another target unit.
    targets: { what: 'unit', prompt: 'Reconfigure: Augment target unit and all of its mods onto another target unit.' },   // TODO check what: unit|any|stackSpell
    run: (g, ctx) => { /* TODO */ },
  },
});

// ── per-card test stub ────────────────────────────────────────────
test('Reconfigure: TODO — Augment target unit and all of its mods onto another target ', () => {
  const h = new Harness(TODO_SEED);
  toDeployment(h);
  // TODO: exercise the printed behavior end-to-end, then assert on state + log.
  // Oracle text: Augment target unit and all of its mods onto another target unit. {i}(The first target must have{/i} [Augment] {i}to be able to be augmented.)
  assert.fail('unwritten per-card test — definition of done');
});
